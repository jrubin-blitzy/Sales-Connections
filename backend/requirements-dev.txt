# --- Sales-Connections backend dev dependencies ---
# Always installed alongside requirements.txt for local development and CI.

-r requirements.txt

# Test runners
pytest==8.4.2
pytest-flask==1.3.0
pytest-cov==6.0.0
pytest-mock==3.15.1
coverage==7.6.10

# Test fixtures / data
factory-boy==3.3.3
faker==33.1.0
freezegun==1.5.5
responses==0.25.8

# Lint / format / static analysis
ruff==0.13.0
mypy==1.18.2

# Type stubs (optional - for mypy)
types-requests==2.32.0.20240914
types-PyYAML==6.0.12.20240917

# Lockfile / dependency mgmt
pip-tools==7.5.1
build==1.3.0

# Mocking / HTTP testing helpers
respx==0.22.0
