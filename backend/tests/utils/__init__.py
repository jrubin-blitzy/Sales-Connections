"""Unit tests for ``app.utils`` pure-function helpers.

Tests in this subpackage are FAST (no DB, no I/O, no network) and form
the foundation of the test pyramid. See ``test_url.py`` and
``test_sanitization.py`` for the per-module test files.
"""
