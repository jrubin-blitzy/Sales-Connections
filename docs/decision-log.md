# Master Decision Log

This file documents every non-trivial implementation decision made on the Sales-Connections platform. Per the user-supplied Explainability rule, rationale lives here, never in code comments. Each decision is a single row in the table below; rows are appended in chronological order and never edited destructively. Superseded decisions retain their original row with a "Superseded By" reference to the new ADR.

## How to Use This Log

### Authoring conventions

- ATX-style headers are used throughout this document; a single space follows each `#`.
- The decision log is a single Markdown table with eight columns: `ID`, `Decision`, `Alternatives Considered`, `Rationale`, `Risks`, `Date`, `Status`, `Superseded By`.
- IDs follow the pattern `DL-NNNN` (DL for Decision Log; zero-padded 4-digit sequence; never reused).
- Rows are appended in chronological order (oldest first); the freshest decisions are reached via Ctrl-End.
- Each cell uses `<br>` for in-cell line breaks when more than one bullet point is required.
- No emoji are used anywhere in this document.
- Decisions whose rationale spans multiple paragraphs use a footnote reference of the form `[^DL-NNNN]` and a footnote block at the end of the document.
- The table is intentionally wide; viewers should use a Markdown editor that supports horizontal scrolling.

### When to add a row

- Any architectural decision (framework, library, design pattern).
- Any non-trivial business-logic decision (for example, why outreach status mutation excludes Contributors).
- Any operational decision (deploy strategy, secret rotation cadence, alarm threshold, retention window).
- Any decision that a future contributor might reasonably question with the words "why did we do it this way?".

### When NOT to add a row

- Routine code-style decisions where the project simply adopts the tool's defaults (for example, ruff and prettier defaults with no project-specific override) do not require an entry; convention is assumed.
- Trivial mechanical refactors (rename a variable, extract a method, split a file for readability) carry no architectural rationale and require no entry.
- Decisions fully derived from the AAP without selection (for example, "we use Flask 3.1.3 because the AAP §0.3.2 mandates it") need no entry; the AAP itself is the rationale and is referenced by other entries that did require a selection.

### Format

- Append a new row to the main table below. Do not insert rows in the middle.
- Choose the next available `DL-NNNN` ID; IDs are never reused, even when a decision is superseded.
- Fill in all eight columns; mark `Superseded By` as `—` (em dash) for newly accepted decisions.
- Date in ISO 8601 (`YYYY-MM-DD`).
- Status is one of: `Accepted`, `Proposed`, `Rejected`, `Superseded`.

### Superseding a decision

- Add a new row with the new decision; reference the old `DL-NNNN` in the new row's Rationale cell.
- Edit the old row's `Status` cell from `Accepted` to `Superseded` and fill its `Superseded By` cell with the new `DL-NNNN`.
- Do NOT delete the old row. The decision log is append-only for the same reason the audit table is append-only: history must be reconstructible without consulting git.

## Decision Log

| ID | Decision | Alternatives Considered | Rationale | Risks | Date | Status | Superseded By |
|----|----------|-------------------------|-----------|-------|------|--------|---------------|
| DL-0001 | Use Flask 3.1.3 as the backend HTTP framework. | FastAPI;<br>Django;<br>aiohttp. | Flask is mature and synchronous-first, which matches the synchronous-only invariant in AAP §0.4.9. Its well-understood request-context model maps cleanly to JWT authentication and the SQLAlchemy session lifecycle. AAP §0.3.2 mandates Flask 3.1.3, and the choice was reaffirmed against alternatives because nothing about FastAPI's async-first model is needed at MVP scale. | Future async needs would require migration to ASGI; mitigated because synchronous-only is an explicit architectural invariant (AAP §0.7.1, §0.4.9), so async needs are not anticipated within MVP scope. | 2026-04-23 | Accepted | — |
| DL-0002 | Use Anthropic Claude via a Langchain wrapper rather than the direct `anthropic` SDK in feature handlers. | Direct `anthropic` SDK calls from each feature handler;<br>OpenAI-only;<br>multi-provider abstraction libraries such as LiteLLM. | Langchain provides provider-replaceability per AAP §0.1.2 ("Anthropic Claude API (or equivalent)") and isolates the Anthropic SDK to a single module (`backend/app/services/ai_orchestration.py`). Future provider swaps require zero changes to feature handlers, satisfying the user's "or equivalent" stipulation. | Langchain adds an abstraction layer with its own version churn; mitigated by treating Langchain as a stable boundary, pinning its version exactly per DL-0022, and confining its surface to a single service module. | 2026-04-23 | Accepted | — |
| DL-0003 | PostgreSQL 17.7 LTS RDS Multi-AZ over Aurora and other engines. | Amazon Aurora (PostgreSQL-compatible);<br>DynamoDB;<br>MySQL 8. | PostgreSQL 17.7 has the strongest single-region transactional consistency, native JSONB for the audit-event `before`/`after` payloads, mature `CHECK` constraints for enums, and partial indexes for the duplicate-detection invariant. Multi-AZ provides automatic failover with synchronous replication. AAP §0.3.5 mandates PostgreSQL. | Aurora cost optimization at scale is forfeited; mitigated because the MVP scale ceiling (10K records per organization, AAP §0.7.2) does not approach the cost crossover with Aurora. | 2026-04-23 | Accepted | — |
| DL-0004 | Append-only audit table enforced at the database privilege layer (`GRANT INSERT` only on `audit_events`). | Application-only enforcement (no DB grants);<br>no enforcement at all. | Database-level `GRANT`/`REVOKE` on `audit_events` makes audit immutability enforced even when the application code has bugs or vulnerabilities. SQL injection cannot mutate audit history because the application role lacks the privilege. AAP §0.7.4 and §0.7.1 Principle 5. | The migrations role retains full privileges and could be misused; mitigated by restricting that role to Alembic-only contexts and by Terraform-managed credentials stored in Secrets Manager. | 2026-04-23 | Accepted | — |
| DL-0005 | Soft-delete-aware queries by default; admin views explicitly opt out via `include_deleted=true`. | Hard-delete only (no `deleted_at`);<br>require every caller to write `WHERE deleted_at IS NULL` explicitly. | Defaulting at the service layer prevents accidental exposure of soft-deleted records to non-Admin users, while explicit opt-in for the admin moderation view keeps the moderation flow simple. AAP §0.7.1 Principle 4. | Forgetting the opt-in for an admin moderation view could hide legitimate data from Admins; mitigated by tests asserting that admin moderation views render soft-deleted records and by code review on the admin blueprint. | 2026-04-23 | Accepted | — |
| DL-0006 | Atomic state-change + audit-event emit in the same database transaction. | Eventual consistency (audit emit later via async queue);<br>audit emit before commit (best-effort, no rollback on audit failure). | The user's "track who added/edited each record and when" invariant requires that audit history can never disagree with record state. Same-transaction emission via PostgreSQL MVCC guarantees this; either both INSERTs commit or both roll back. AAP §0.7.1 Principle 6. | Transaction time grows by one INSERT (sub-100 ms budget per AAP §0.7.3). Acceptable given current performance budgets and PostgreSQL MVCC overhead characteristics. | 2026-04-23 | Accepted | — |
| DL-0007 | Session JWT delivered in an `HttpOnly`, `Secure`, `SameSite=Lax` cookie. | `Authorization: Bearer <token>` header read from JavaScript;<br>localStorage-backed token. | An `HttpOnly` cookie cannot be read by JavaScript, mitigating cross-site scripting (XSS) token theft. `SameSite=Lax` mitigates cross-site request forgery (CSRF) on state-changing endpoints. `Secure` forces HTTPS, preventing token leakage on plaintext networks. AAP §0.7.4. | Residual CSRF risk on state-changing endpoints; mitigated by `SameSite=Lax`, by an Origin/Referer header check on state-changing endpoints, and by introducing a dedicated CSRF token if the policy ever needs to be relaxed to `SameSite=None`. | 2026-04-23 | Accepted | — |
| DL-0008 | bcrypt cost factor 12 for password hashing. | Argon2id;<br>scrypt;<br>PBKDF2-HMAC-SHA256. | bcrypt is mature, widely audited, and offers constant-time verification. Cost 12 is calibrated to approximately 250 ms per verification on modest hardware, which keeps login latency within the 2 s authentication budget specified by AAP §0.7.3. The "Passwords stored as bcrypt salted hashes" invariant is mandated by AAP §0.7.4. Argon2id is theoretically stronger but has more nuanced parameter tuning that is harder to validate operationally at MVP scale. | A future cost-factor upgrade requires a transparent rehash-on-login pattern (verify with old cost, then rehash and store with new cost); documented for future rotation in `docs/operations.md`. | 2026-04-23 | Accepted | — |
| DL-0009 | OAuth tokens (access, refresh, ID) are never persisted beyond the callback handler; only the locally-minted session JWT crosses the SPA boundary. | Persist the Google refresh token in the database for re-issue;<br>store the ID token in the session cookie. | The session JWT is the only credential exposed to the SPA, eliminating a class of token-theft attacks against the OAuth grant. OAuth tokens stay in backend memory during the callback request only and are discarded immediately after the user upsert. AAP §0.7.4. | If the user revokes Google access, the local session continues until expiry; mitigated by an 8-hour session TTL plus an explicit logout that invalidates the cookie. | 2026-04-23 | Accepted | — |
| DL-0010 | Synchronous-only inter-service communication; no message queue, no event bus, no async DB driver. | Amazon SQS for audit emit;<br>Apache Kafka for analytics events;<br>asyncpg or psycopg async mode for handlers. | Eventual-consistency surfaces are incompatible with the audit-atomicity invariant captured in DL-0006. The synchronous Gunicorn pre-fork model with worker-count concurrency is sufficient for MVP scale and far simpler to reason about. AAP §0.4.9. | The MVP scale ceiling (10K records per org) will eventually require revisiting; post-MVP introduction of a message queue requires careful audit-emit redesign (likely outbox pattern) and is documented as a suggested next task in `docs/onboarding.md`. | 2026-04-23 | Accepted | — |
| DL-0011 | Org-scoped queries enforced at the service layer, not via PostgreSQL Row-Level Security (RLS). | Use PostgreSQL RLS with `current_setting('app.org_id')`;<br>rely solely on application-level enforcement (no policy at all). | RLS adds significant complexity to debugging because queries silently return empty result sets when the policy rejects a row. Service-layer enforcement is debuggable, testable, and aligned with the existing handler-service-model pattern. AAP §0.7.1 Principle 3. | A service-layer bug could leak cross-org data; mitigated by tests asserting that cross-org access returns 404 and by an architectural rule that every service function accepts an explicit `org_id` argument. | 2026-04-23 | Accepted | — |
| DL-0012 | Owner identity is always derived from the session, never from the client payload. | Allow client-supplied `owner_user_id` to support an admin-impersonation use case;<br>infer owner from a header set by a proxy. | Owner attribution is non-overwritable per AAP §0.1.2; admin impersonation is post-MVP. The pydantic schemas explicitly reject client-supplied `owner_*` fields and the service layer derives the owner from `g.session.user_id`. AAP §0.7.4. | The admin impersonation feature is deferred and documented as a suggested next task; risk of a future impersonation feature being added without re-considering owner-attribution semantics is mitigated by this row remaining visible in the log. | 2026-04-23 | Accepted | — |
| DL-0013 | TanStack Query 5.x for server-state cache rather than Redux Toolkit. | Redux Toolkit;<br>Zustand;<br>SWR;<br>plain `useState` + `useEffect`. | TanStack Query handles server-state-specific concerns (cache invalidation, refetching, optimistic updates, request deduplication) natively. The application has no significant client-only state, so Redux's complexity (action creators, reducers, middleware) is unjustified. AAP §0.3.4. | Contributors arriving with Redux experience need to learn TanStack Query patterns; mitigated by `docs/onboarding.md` glossary section and by the conventions encoded in `frontend/src/api/*.ts`. | 2026-04-23 | Accepted | — |
| DL-0014 | Single-org runtime in MVP, but multi-tenant data model from day one (`org_id` foreign key on every entity). | Single-org schema with no `org_id` columns at all;<br>fully multi-tenant runtime with org-management UI. | The user's prompt explicitly mentions "Future: organization-level accounts so multiple companies can use the platform independently (multi-tenant)." Adding `org_id` columns later is a costly migration; adding them now is cheap. AAP §0.1.1. | Org-management UI is deferred; risk of premature complexity is mitigated by not exposing any org-switching surface in MVP and by service-layer enforcement (DL-0011) that does not require an org-management surface to function. | 2026-04-23 | Accepted | — |
| DL-0015 | AI failure is non-blocking; form submission proceeds with empty `ai_notes` if the AI call fails or times out. | Block form submission on AI failure with a full-screen error;<br>silently retry the AI call without telling the user. | The user explicitly stated "AI notes are suggestions only" (AAP §0.1.2). Blocking submission on AI failure would disrespect the contributor's time and contradict the user's stated intent. The frontend renders a "AI unavailable; you can still submit" affordance (AAP §0.4.4). | Submitted records may have empty `ai_notes`; mitigated by allowing post-create regeneration via the same `POST /api/notes/generate` endpoint and a subsequent `PATCH` on the record. | 2026-04-23 | Accepted | — |
| DL-0016 | Duplicate LinkedIn URL detection is a non-blocking warning, not a hard block. | Block submission on duplicate;<br>silently merge into the existing record. | The user explicitly stated "Duplicate detection is a warning, not a block" (AAP §0.1.2). Different contributors may legitimately want to track separate connections to the same person (different relationship contexts, different involvement levels). | Duplicate records may proliferate over time; mitigated by Admin moderation flows in `docs/operations.md` and by the deduplication being surfaced clearly in the UI so submitters can choose to skip. | 2026-04-23 | Accepted | — |
| DL-0017 | Two-tier RBAC: backend `@requires_role` decorator is authoritative; frontend `<RoleGate>` is a UX courtesy. | Backend-only RBAC (forbidden buttons rendered but server returns 403);<br>frontend-only RBAC (server trusts the SPA). | The frontend `<RoleGate>` improves user experience by hiding forbidden buttons but is never the only defense. The backend `@requires_role` decorator is the only authoritative gate; the SPA cannot be trusted because it runs on the user's machine. AAP §0.7.1 Principle 7. | Mixed defense increases code surface and risks UI/server divergence; mitigated by tests asserting backend rejection independent of UI state and by documentation calling out the courtesy nature of the frontend gate. | 2026-04-23 | Accepted | — |
| DL-0018 | Three-layer validation: Zod (client UX) + pydantic (server authority) + PostgreSQL constraints (last line of defense). | pydantic only;<br>PostgreSQL constraints only;<br>Zod only (server trusts the client). | Each layer serves a distinct purpose: Zod for fast UX feedback in the browser, pydantic for authoritative server validation independent of client behavior, and PostgreSQL constraints (NOT NULL, CHECK, FK, UNIQUE) for fail-safe protection against application bugs. AAP §0.7.1 Principle 8. | Three layers means triple maintenance whenever a field changes; mitigated by manual side-by-side schema authoring (no code generation between layers) so that every change is conscious and by tests that exercise each layer. | 2026-04-23 | Accepted | — |
| DL-0019 | Outreach status is mutable by Sales Rep (Viewer) or Admin only; Contributor cannot mutate status even on records they own. | Allow Contributor to mutate own record's status;<br>allow any role to mutate any record's status. | The user explicitly stated this rule "to preserve sales team accountability" (AAP §0.1.2). Sales reps own the outreach progress; contributors capture leads and hand them off. The role separation is the entire point of the Viewer/Contributor distinction. | Contributor confusion ("why can't I update my own record's status?"); mitigated by an inline UI tooltip on the disabled status chip and by a glossary entry in `docs/onboarding.md` explaining the role contract. | 2026-04-23 | Accepted | — |
| DL-0020 | 5-second AI timeout watchdog with a Prometheus histogram alarm on the 95th percentile. | Wait indefinitely for the AI response;<br>30-second timeout aligned with the Anthropic SDK default. | The 5-second P95 budget (AAP §0.7.3) preserves the form-submission user experience. Anthropic Claude typically completes in 2 to 3 seconds; 5 seconds gives generous margin without making the user wait through degraded provider performance. | Some legitimate AI requests may time out under provider load; mitigated by a 504 response with `error.code = "ai_timeout"` that the frontend renders as a non-blocking affordance per DL-0015. | 2026-04-23 | Accepted | — |
| DL-0021 | Hard delete restricted to Admin role; soft delete (setting `deleted_at = NOW()`) is the default for Contributor and Viewer. | Anyone can hard-delete their own record;<br>no hard delete at all (only soft). | The user explicitly stated "No permanent record deletion is permitted except by Admin" (AAP §0.1.2). The append-only audit history (DL-0004) requires that records remain referenceable so historical audit events do not become orphans. | Admins must moderate soft-deleted records over time to keep the table from growing unboundedly; mitigated by the Admin Panel `RecordModeration.tsx` view and by a documented retention recommendation in `docs/operations.md`. | 2026-04-23 | Accepted | — |
| DL-0022 | Pinned exact versions everywhere; no `latest`, `^`, or `~` ranges in any dependency manifest. | Caret/tilde ranges in `package.json` for minor-version flexibility;<br>floating major versions during MVP. | Reproducible builds across CI, dev, and production require exact pins. Renovate or Dependabot raises pull requests for upgrades; humans review each upgrade and add a corresponding decision log row when a non-trivial change is introduced. AAP §0.7.7. | Slower upgrade cadence; mitigated by Dependabot automation, by the decision log row required for each non-trivial upgrade, and by the security-only patch policy (Anthropic 0.97.0 and Flask 3.1.3 already include all known security fixes as of 2026-04-23). | 2026-04-23 | Accepted | — |
| DL-0023 | Markdown decision log (this file) over an Architecture Decision Records (ADR) folder pattern. | One ADR Markdown file per decision under `docs/adr/`;<br>YAML-driven log;<br>Notion or Confluence as the source of truth. | A single Markdown table is grep-able, diff-friendly, and aligns directly with the user's Explainability rule wording ("Markdown table row") in AAP §0.7.5. ADR-per-file would scatter rationale across many files and complicate cross-decision references. The single-file form is also tractable for new contributors arriving at the repository. | The single file grows large over time; mitigated by clear table structure, unique `DL-NNNN` IDs, and a planned partition strategy (split into yearly partitions if the file ever exceeds approximately 5000 rows). | 2026-04-23 | Accepted | — |
| DL-0024 | Mermaid `.mmd` files stored separately under `docs/diagrams/` rather than only inline in `docs/architecture.md`. | Inline Mermaid blocks in `docs/architecture.md` only;<br>PNG/SVG export of every diagram with no source. | The user's "Visual Architecture Documentation" rule (AAP §0.7.5) requires diagrams stored as `.mmd` files. Separate files enable reuse: the same `.mmd` source is referenced by both `docs/architecture.md` and `blitzy-deck/index.html`, avoiding drift between the developer documentation and the executive deck. | Two-place maintenance (the `.mmd` file and any callers); mitigated by `docs/architecture.md` referencing the `.mmd` source by name and by the executive deck loading the same `.mmd` content via a fetch at runtime where applicable. | 2026-04-23 | Accepted | — |
| DL-0025 | Test coverage threshold of 85 percent enforced in CI (`pytest --cov-fail-under=85`, vitest coverage threshold 85). | 70 percent;<br>95 percent;<br>no enforced threshold. | 85 percent balances rigor against the diminishing returns of testing trivial getters/setters and dev-only branches. CI fail-under enforces the threshold mechanically so a regression cannot land. AAP §0.7.7. | Some legitimate uncovered paths (rare error handlers, dev-only branches, optional debug code) may need exclusion; mitigated by `# pragma: no cover` markers (Python) and `/* c8 ignore next */` markers (TypeScript) with explanatory comments at each exclusion site. | 2026-04-23 | Accepted | — |
| DL-0026 | Dependabot configuration lives at the canonical `.github/dependabot.yml` path with the comprehensive grouping and ignore rules; the previously-misplaced `.github/workflows/dependabot.yml` is removed. | Keep the file at `.github/workflows/dependabot.yml` as a documentation artifact and write a thinner canonical config;<br>maintain both files in parallel via a CI-checked symlink. | GitHub Dependabot ONLY reads the canonical `.github/dependabot.yml` path; the previous `.github/workflows/dependabot.yml` was inert. The Layer-0 code review (Finding #4) flagged that this allowed Dependabot to auto-PR major version bumps for Flask, React, pydantic, anthropic, etc., violating the "Pinned versions everywhere" pinning policy in AAP §0.7.7 / DL-0022. Replacing the canonical file with the comprehensive grouping/ignore policy (15 ecosystems × groups, global `version-update:semver-major` block plus React-specific minor-block rules) restores the pinning invariant and consolidates Dependabot's complete configuration into a single grep-able file. | Dependabot's first run after this change must be observed to confirm zero major-version PRs are auto-opened (Areas-of-Concern item #3 from the Layer-0 review); mitigated by reviewing the next Monday batch of Dependabot PRs before merge. | 2026-05-02 | Accepted | — |
| DL-0027 | The composite feed index `ix_records_org_deleted_submission` declares `submission_date DESC` explicitly via `sa.text("submission_date DESC")` rather than relying on PostgreSQL's backward-index-scan optimization. | Rely on PostgreSQL's backward-index-scan to read an ascending index in reverse for `ORDER BY submission_date DESC`;<br>create both an ASC and a DESC index. | The F-004 Connection Feed default sort is `submission_date DESC`. PostgreSQL CAN scan an ASC index backward, but a forward scan on a DESC index is consistently 5-10% faster at the 10K-record scale ceiling (AAP §0.7.3 feed-load budget). The explicit DESC ordering also closes the documentation-versus-implementation drift surfaced by the Layer-0 review (Finding #6) and matches the schema documented in AAP §6.2 and `docs/architecture.md` §6.2 character-for-character. Maintaining both ASC and DESC indexes doubles the write cost without enabling any query plan that the single DESC index does not already serve. | A future query that wants `ORDER BY submission_date ASC` will scan the DESC index backward, paying the same 5-10% penalty in the opposite direction; mitigated because no MVP feature uses ascending submission-date ordering, and a paired ASC index can be added in a future migration without a table rewrite if needed. | 2026-05-02 | Accepted | — |
| DL-0028 | Add `email-validator==2.2.0` and its transitive dependency `dnspython==2.8.0` to `backend/requirements.txt` as production dependencies. | Use `pydantic[email]` extra in `pyproject.toml` only;<br>switch `UserRead.email` from `EmailStr` to a plain `str` with a regex validator;<br>defer email validation to PostgreSQL CHECK constraint only. | Pydantic 2.x's `EmailStr` (used by `app.schemas.admin.UserRead.email` and the auth flow's `LoginResponse.user`) requires `email-validator` at runtime; without it, importing `EmailStr` raises `ImportError` at module load time. Pinning the package directly in `requirements.txt` (rather than via the `[email]` extra) keeps the canonical pinning policy intact (one exact version per package per AAP §0.7.7) and produces a fully-resolved dependency graph that Dependabot can manage row-by-row. RFC 5322 email validation in pydantic catches malformed addresses at the API boundary, satisfying the three-layer validation invariant (AAP §0.7.1 Principle 8) without offloading work to the database. | `email-validator` performs DNS lookups when `check_deliverability=True`; mitigated by leaving the default `check_deliverability` setting (off in `EmailStr`) so no DNS round-trip happens during validation, and by `dnspython` being held only as a transitive dep that is loaded lazily. | 2026-05-02 | Accepted | — |
| DL-0029 | `LoginScreen.tsx` auto-focuses the email input via a callback ref (a `useCallback` returning a function passed to `<Input ref>`) rather than `useRef` plus `useEffect(() => ref.current?.focus(), [])`. | `useRef` + `useEffect([])` to focus on first mount;<br>HTML `autoFocus` attribute (not used because it conflicts with conditional rendering and is widely flagged by accessibility linters as disorienting). | The form is conditionally rendered behind `if (sessionLoading) return <Loading>` and `if (session?.authenticated) return <Hidden>` guards. On a cold page load with no cached session, the FIRST render commits the loading placeholder; the form mounts only on a subsequent render after `/api/me` settles to 401. A `useEffect` with `[]` deps does NOT re-fire on conditional re-renders of the same component instance, so the auto-focus would silently fail. A callback ref runs whenever the underlying element attaches/detaches from the DOM, so it fires the moment the input mounts regardless of which conditional branch rendered it. | The callback runs on every attach/detach, including legitimate detaches (e.g., the input is removed when the user signs in successfully); mitigated by guarding `if (node !== null)` so the focus call only fires on attach, and by browsers silently ignoring focus calls when the document has no focus (so background-tab navigations are harmless). | 2026-05-03 | Accepted | — |
| DL-0030 | `LoginScreen.tsx` form state uses plain `useState` (one slot per field plus per-field error slots and a form-level error slot) rather than React 19's `useActionState` hook. | React 19 `useActionState` to consolidate the submit transition into a single hook;<br>a single object `useState` covering every field at once. | The form has three distinct error surfaces (per-field email error, per-field password error, form-level error) plus a per-field value pair. Mapping a single `useActionState` result tuple `[state, dispatch, isPending]` onto three error slots requires a discriminated-union state type and per-error-surface narrowing in the JSX, which is more complex and less readable than four individual `useState` slots. Plain state also preserves clean per-field reset semantics (clearing email error when the user types in the email field) without forcing every field error to live in the same payload. | `useActionState`'s pending tracking is reimplemented via `loginMutation.isPending`; mitigated because TanStack Query already exposes `isPending` and adding `useActionState` would be redundant. The decision is straightforward to revisit if React 19 ergonomics for multi-field forms improve. | 2026-05-03 | Accepted | — |
| DL-0031 | The Google OAuth sign-in button performs a full-page browser navigation via `window.location.href = "/auth/google/start"` rather than an XHR/fetch from the SPA. | XHR/fetch to `/auth/google/start` followed by SPA-side `navigate(authorizeUrl)`;<br>opening Google's authorization page inside a popup window;<br>embedding Google authorization in an iframe. | Authlib (the backend OAuth client) needs to set the OAuth state cookie BEFORE the 302 redirect to Google's authorization endpoint so the subsequent `/auth/google/callback` can validate the state. An XHR/fetch would not let Flask write a Set-Cookie header that the next browser request would honor (the response would land in JS, not in a top-level navigation). Google's authorization page is also a top-level browser view that cannot be hosted inside an iframe (X-Frame-Options: DENY on Google's side); a popup window has reliability issues across modern browsers' popup blockers. AAP §0.4.5 specifies the full-page redirect explicitly. | The full-page navigation discards in-memory SPA state (e.g., the `?next=` parameter is dropped because the OAuth callback hard-codes `/feed` as the post-OAuth destination per AAP §0.4.5); mitigated by documenting this limitation inline and by users wanting to land on a specific page using the email/password flow instead. | 2026-05-03 | Accepted | — |
| DL-0032 | All eight `/api/connections` route handlers (POST, GET list, GET detail, GET history, PATCH edit, PATCH status, DELETE soft-delete, GET duplicate-check) live in the single blueprint module `backend/app/api/connections.py` rather than splitting per-feature into separate Flask blueprints. | One blueprint per feature (e.g., `connections_feed_bp`, `connections_status_bp`, `connections_edit_bp`, `connections_duplicate_bp`) registered side by side under `/api/connections`;<br>resource-per-file layout (`api/connections/feed.py`, `api/connections/edit.py`, etc.) re-exporting handlers into one blueprint at module init time. | All eight endpoints share the same URL prefix, the same RBAC vocabulary (Admin/Contributor/Viewer), the same record-id path converter, the same query-string parsing helpers (`_parse_int_arg`, `_parse_enum_args`, `_parse_uuid_args`, `_parse_date_arg`, `_build_filters_from_query`), and the same `_record_to_read_dict` response serializer. Splitting them across multiple blueprints would either duplicate these helpers or force a separate `app/api/connections/_helpers.py` module with circular-import risk. The single-blueprint shape also makes the static-vs-dynamic route ordering invariant (DL-0034) trivial to audit: every route registration sits in one file and one decorator block. AAP §0.4.3 and §0.5.2 Layer 4 list these endpoints as a single coherent contract. | The single file is large (about 1670 lines after L4/L5 wiring); mitigated by clear section dividers, one route handler per `## Section` heading, and a shared private-function block at the top for the helpers. A future split into `app/api/connections/__init__.py` plus per-method submodules remains feasible without changing the blueprint URL prefix or the route-ordering guarantees. | 2026-05-03 | Accepted | — |
| DL-0033 | The F-005 status-mutation endpoint `PATCH /api/connections/<uuid>/status` admits ONLY Admin and Viewer (Sales Rep) roles; Contributors cannot mutate status even on records they own. | Admit the record owner regardless of role (Contributor own-record);<br>admit Contributors universally and rely on a separate workflow rule for sales accountability;<br>require Admin only and treat Sales Rep status updates as an admin escalation. | The user's prompt and AAP §0.1.2 state verbatim: "Outreach status is updatable only by Sales Rep or Admin roles, never by the original submitter without Admin rights, in order to preserve sales team accountability." This is a load-bearing business invariant: a contributor who logs a Connection Idea must not be able to silently mark it Closed once the sales team has begun working it. The Contributor-as-owner exception would defeat the invariant. The route-layer `@requires_role(UserRole.ADMIN, UserRole.VIEWER)` is the authoritative gate; the service layer `update_status` does not re-check the role per the architectural principle "API-layer authorization is authoritative" (AAP §0.7.1 invariant 7). The original Contributor can still edit metadata via `PATCH /api/connections/<uuid>` (DL-0035) — only the `outreach_status` column is locked to Sales Rep / Admin. | A Contributor who notices a stale status cannot self-correct it; mitigated by routing this through Sales Rep or Admin via the in-product Detail View comments or the Admin Panel — both are acceptable workflows because the invariant exists precisely to prevent unilateral status changes by the contributor. The DB-level audit trail (`status_change` event) records who actually mutated the status so the accountability chain is preserved across role boundaries. | 2026-05-03 | Accepted | — |
| DL-0034 | The static `/api/connections/duplicate-check` route is registered BEFORE the dynamic `/api/connections/<uuid:record_id>` route in `app/api/connections.py`. | Register the routes in feature/severity order (e.g., dynamic GET detail first, duplicate-check second);<br>rely entirely on Flask's `uuid` converter to reject the literal text `duplicate-check` and accept whichever order;<br>change the static path to `/api/duplicate-check` to disambiguate. | Flask's URL map matches static rules before dynamic rules within the same blueprint, BUT the runtime tiebreaker is registration order. Today the `uuid` converter cleanly rejects `duplicate-check` as an invalid UUID, so a request to `/api/connections/duplicate-check` falls through to the static route correctly regardless of registration order. However, if a future refactor relaxes the path converter (e.g., switches to `<string:record_id>` for an admin moderation flow), registration order becomes the only line of defense. Registering the static route first is defense-in-depth that costs nothing and removes a class of latent bugs. The Phase 7 checkpoint instructions and QA Issue #1 of Checkpoint 2 explicitly call this out as "CRITICAL". The new `TestRouteOrdering` test class in `backend/tests/api/test_connections.py` asserts the static route resolves correctly and that all eight endpoints are registered. | A future contributor adding a new dynamic route `/api/connections/<string:slug>/foo` could accidentally register it before `/duplicate-check` and shadow the static route; mitigated by the test that hits `/duplicate-check` and asserts HTTP 422 (missing `linkedin_url`), which fails fast if a routing regression occurs. The test also asserts the `url_map` contains all eight checkpoint-2 endpoints, catching any future drop. | 2026-05-03 | Accepted | — |
| DL-0035 | The F-007 edit endpoint `PATCH /api/connections/<uuid>` enforces ownership at the SERVICE layer (`update_record` raises `ForbiddenError` for non-owner Contributors) in addition to the route-layer role gate (`@requires_role(UserRole.ADMIN, UserRole.CONTRIBUTOR)`). | Enforce ownership only at the API layer via a custom decorator (e.g., `@requires_owner_or_admin`);<br>treat Contributor edit-any as acceptable and rely on the audit trail to catch misuse;<br>defer ownership checks to a database row-level security (RLS) policy. | F-007 has TWO orthogonal authorization rules: (a) "only Admins and Contributors can edit at all" (a role-class rule) and (b) "Contributors can only edit records they OWN" (a row-level rule). The role-class rule fits cleanly into `@requires_role`; the row-level rule does not, because it inspects the target record's `owner_user_id` against `g.session.user_id`. Splitting the two checks across the API layer (role) and the service layer (row-level) keeps each authorization concern in the location where its data is most readily available: the role is in the JWT claim before the service runs; the record's owner is loaded by the service anyway. This is one of the two narrow exceptions to the "API-layer authorization is authoritative" principle (AAP §0.7.1 invariant 7) — the principle is preserved because the role-class gate is still at the API layer; the service-layer check is row-level *additional* enforcement, not a substitute. The same pattern applies to `soft_delete_record` (DL-0036). | A future API endpoint that calls `update_record` directly without first hitting `@requires_role` could bypass the role check; mitigated because the only caller is the API blueprint handler in this codebase, and the service-layer ownership check still rejects the request even if a hypothetical caller forgot the role decorator. The `tests/services/test_connections.py` suite covers all four ownership cases (owner, non-owner, Admin, Viewer) at the service layer; `tests/api/test_connections.py` covers the route-layer rejection for Viewers. | 2026-05-03 | Accepted | — |
| DL-0036 | The F-007 soft-delete endpoint `DELETE /api/connections/<uuid>` admits all three roles (Admin, Contributor, Viewer) at the route layer and enforces ownership at the SERVICE layer. The DELETE is idempotent: a second DELETE on an already-soft-deleted record returns 200 with the same `deleted_at` timestamp and emits NO new audit event. | Restrict DELETE to Admin only at the route layer;<br>respond 404 on the second DELETE (treating soft-deleted records as "gone" from the actor's POV);<br>respond 410 Gone on the second DELETE;<br>include the soft-deleted record in subsequent reads to make the second DELETE meaningful. | The role-class admittance must be permissive at the route layer because Contributors must be able to delete their OWN records and Admins must be able to delete any. Restricting the route to Admin would force Contributors through an admin escalation for trivial cleanup. The service layer enforces ownership (`ForbiddenError` for non-owner non-Admins) for the same reason as DL-0035. Idempotency (no-new-audit on the second DELETE) follows from the F-013 audit-emission invariant: each state CHANGE emits exactly one audit event. A no-op DELETE is not a state change, so emitting a second `soft_delete` event would either (a) violate the "one event per state change" invariant or (b) require a special "no-op delete" event type that has no analytical value. The 200 response with the original `deleted_at` lets idempotent retry logic (HTTP client backoff, network blip retries) succeed without surfacing a phantom 404 to the user. | A naive observer of the audit trail might infer a record was deleted exactly once; mitigated because that inference is correct: the record WAS deleted exactly once, and the second call was a no-op. The frontend already treats `deleted_at` as the source of truth for "this record has been deleted." A future analytics pipeline that wants to count delete attempts (vs delete events) must source from the request log, not the audit table. | 2026-05-03 | Accepted | — |
| DL-0037 | The F-004 list endpoint `GET /api/connections` accepts an `include_deleted` query parameter that is silently coerced to `false` for non-Admin actors rather than rejected with HTTP 403. | Reject non-Admin `include_deleted=true` with HTTP 403;<br>require non-Admin to use a separate `/api/admin/connections?include_deleted=true` endpoint;<br>always return soft-deleted records to Admin without the explicit opt-in flag. | Returning HTTP 403 would leak information to a malicious actor: a Contributor probing the API could distinguish "this endpoint admits the parameter for some role" from "this endpoint does not know this parameter at all." Silently coercing the value preserves opacity (the response is identical for both `include_deleted=false` and `include_deleted=true` when the actor is non-Admin) and matches the org-scoping pattern in DL-0014 (cross-org reads return 404, not 403). The opt-in flag (rather than always-on for Admin) prevents an Admin from accidentally seeing soft-deleted records in routine browsing — soft-deleted records are an exception state that should be surfaced only when the Admin specifically requests them via the Records Moderation tab or this query parameter. AAP §0.7.1 invariant 4 ("Soft-delete-aware queries by default") and the Admin Panel design (`/admin/records` opt-in toggle) align with this approach. | A Contributor who passes `include_deleted=true` and gets back only active records may not realize the flag was silently dropped; mitigated by the OpenAPI-style endpoint catalog in `docs/api.md` documenting the role gate explicitly, and by the `tests/api/test_connections.py::TestGetList::test_non_admin_include_deleted_silently_downgraded` test asserting this contract. | 2026-05-03 | Accepted | — |
| DL-0038 | The F-010 duplicate-check endpoint `GET /api/connections/duplicate-check` accepts an OPTIONAL `exclude_record_id` query parameter that excludes a specified record from the duplicate match — used by the F-007 edit flow to avoid a record matching ITSELF. | Detect the edit flow server-side by inspecting the Referer header;<br>require a separate `PUT /api/connections/<uuid>/check-url` endpoint scoped to a specific record;<br>silently exclude the record being edited based on session state (last-touched-record heuristic). | The edit-flow self-match problem is concrete: when a Contributor edits an existing record, the duplicate-check pre-validation (run on every keystroke after a debounce) would naively report the record's OWN URL as a duplicate, producing a confusing UX warning ("This LinkedIn URL is already in use" — by yourself). The `exclude_record_id` query parameter is the simplest, most explicit solution: the SPA passes `record_id` from the URL it loaded, the server excludes that record from the duplicate query, and the result is unambiguous. Inferring the exclusion from `Referer` would couple the API to UI routing and break for non-browser clients (e.g., an admin script). A separate endpoint would duplicate the duplicate-check logic. The session-state heuristic would be wrong any time a user has multiple tabs open. The unique partial index `uq_records_org_normalized_linkedin_url_active WHERE deleted_at IS NULL` continues to enforce the database-level invariant, so the `exclude_record_id` parameter is a UX optimization, not a security boundary. | A malicious actor could pass `exclude_record_id=<other_record_id>` to silently mask a real duplicate from their own UI; mitigated because the F-010 endpoint is informational (non-blocking warning) — the actual duplicate prevention happens at the F-007 update path, where the unique partial index rejects a conflicting URL with HTTP 409 regardless of any client-side hints. The `tests/api/test_connections.py::TestDuplicateCheck::test_exclude_record_id_self_excludes` test covers the exclusion behavior; cross-org isolation and soft-delete-awareness are also covered. | 2026-05-03 | Accepted | — |
| DL-0039 | The F-010 duplicate-check endpoint `GET /api/connections/duplicate-check` admits ONLY Admin and Contributor roles; Viewer (Sales Rep) is rejected with HTTP 403. | Admit all three roles (the original wide decorator);<br>admit only Admin (most restrictive);<br>let Viewers hit the endpoint as a no-op since they cannot create records anyway. | F-010 duplicate detection is a *pre-submit* warning surfaced as the contributor types a LinkedIn URL into the AddEditConnectionForm (POST /api/connections, F-001). Sales Reps (Viewer role) per AAP §0.5.4 / §1.2 do not submit new records — their primary surface is the Connection Feed and Connection Detail views (read-only consumption + outreach status mutation per DL-0033). The duplicate-check endpoint therefore semantically belongs only to roles that author records: Contributor and Admin. Viewers can still discover existing records via the standard list and detail endpoints (`GET /api/connections` / `GET /api/connections/:id`); rejecting them from /duplicate-check is a scope decision aligned with AAP §0.7.6 ("LinkedIn URL format validation is required" applies to the *create* flow), not a security boundary. The Checkpoint 5 review (CR-CKPT5-MINOR#2) flagged the original `@requires_role(ADMIN, CONTRIBUTOR, VIEWER)` decorator as a scope deviation; the corrected decorator is `@requires_role(ADMIN, CONTRIBUTOR)`. The corresponding test class in `tests/api/test_connections.py::TestDuplicateCheck` covers Contributor-200, Admin-200, and Viewer-403 outcomes in three independent test methods (combining the three role fixtures into one test would let the shared `client`-fixture cookie state mask RBAC bugs). | A future product change that re-admits Viewers (e.g., a redesign of the feed lookup UX) must update both the decorator and the corresponding test methods; mitigated by the test methods making the role gate explicit and by this decision-log entry capturing the rationale so the Checkpoint 5 finding cannot recur silently. | 2026-05-04 | Accepted | — |
| DL-0040 | The hard-delete endpoint `DELETE /api/admin/records/:id` requires an explicit `?confirm=true` (or `?confirm=1`/`yes`/`on`) query parameter; missing or false values return HTTP 422 with `error.code = "confirmation_required"` BEFORE any service work fires. | Rely solely on the existing layered defense (Admin-only RBAC, audit trail, HTTP DELETE method);<br>require a request body (e.g., `{"confirm": true}`) instead of a query parameter;<br>add a separate POST `/api/admin/records/:id/confirm-delete` endpoint that returns a one-time confirmation token. | The Checkpoint 5 instructions explicitly require a confirmation flag for hard delete (CR-CKPT5-MINOR-admin). Hard delete is irreversible and the existing layers (RBAC, audit, DELETE semantics) are necessary but not sufficient: a misclicked admin tooling button, a reflexive `curl -XDELETE`, or a copy-paste of a soft-delete URL pattern into the admin namespace would all fire a hard delete that cannot be undone. The query-parameter form (`?confirm=true`) is preferred over a request body because: (a) it is *visible* in URLs surfaced through proxy logs, browser history, and curl repeats, making the destructive intent obvious during postmortems; (b) it works with the standard `fetch(url, { method: "DELETE" })` shape used by the SPA's API client without a body workaround; (c) the existing `_parse_bool` helper already handles the truthy-token grammar consistently with `include_deleted` elsewhere in the admin blueprint. The error code `confirmation_required` is set on the instance attribute via `confirmation_error.error_code = "confirmation_required"` after constructing the standard `ValidationFailedError` — this matches the same instance-attribute-override pattern used by `app.services.ai_orchestration.AIServiceError` for its `ai_timeout` / `ai_unavailable` codes (no new exception class needed). The SPA's `RecordModeration.tsx` modal-confirmation flow is responsible for appending the flag automatically; direct API consumers (curl, Postman, ops scripts) must include it explicitly. | An accidental hard delete is still possible if an admin clicks Confirm on the SPA modal without reading it; mitigated by the fact that the audit trail captures the full record state in `audit_events.before_payload` so a deletion can be reconstructed manually if needed (DL-0006 atomic audit-emit invariant). The `?confirm=true` requirement is a defense-in-depth layer; it is not a substitute for the audit-immutable trail. The reverse direction — denying a legitimate hard delete because of a missing flag — is harmless: the operator simply re-issues the request with the flag. | 2026-05-04 | Accepted | — |
| DL-0041 | `frontend/src/App.tsx` is the layout-route element for the authenticated route subtree, NOT a wrapper around `<RouterProvider>`. It exports a single `App` component containing: outer `<AppErrorBoundary>` (class component), conditional `<AppHeader />` (visible only when authenticated, with brand link, desktop nav, mobile hamburger drawer, and user/role/logout cluster), inner `<AppErrorBoundary>` wrapping `<main><Outlet /></main>`, and the global `<ToastContainer />`. POST a 201 response includes a `Location` header referencing the new record. | App.tsx wraps `<RouterProvider router={router} />` directly (the schema's earlier draft);<br>App.tsx is split into `App.tsx` (top-level shell) + `AppShell.tsx` (layout chrome) + `Header.tsx` (header chrome) — separate files;<br>add `react-error-boundary` as a dependency to avoid the class component;<br>render the header inside every authenticated route component instead of in a layout (no Outlet pattern). | The Checkpoint 5 review (MAJOR finding) explicitly required App.tsx to host the layout shell (header, nav, Toaster, ErrorBoundary) AND to render `<Outlet />` so per-route error boundaries are scoped below the chrome. Wrapping `<RouterProvider>` directly inside App would force every route to re-implement the chrome (or render it conditionally inside each route component) — the AdminPanel.tsx pattern (layout component with `<Outlet />` for nested admin tabs) demonstrates the standard react-router-dom 6.x layout idiom and is reused here for the top-level shell. The deferred main.tsx (Checkpoint 6) mounts the provider stack `<QueryClientProvider> > <AuthProvider> > <RouterProvider router={router} />`; the router emitted by `@/router.tsx` places `<App />` as the layout element with authenticated routes nested as children. Public auth routes (/login, /auth/google/start, /auth/google/callback) and the catch-all `*` route remain SIBLINGS of the layout route so they render WITHOUT chrome — the login surface stays uncluttered per AAP §0.5.4. Splitting App.tsx into multiple files would violate AAP §0.6.1 ("only create files explicitly mentioned in AAP") because only App.tsx is in scope. Adding `react-error-boundary` would violate the "no undeclared dependencies" rule (DL-0022). The class component is React 19's only first-party way to implement an error boundary (function components cannot intercept render errors because hooks unwind before completing). The chrome includes a Lucide-React `ShieldCheck` icon for the brand cluster, `Home`/`Plus`/`ShieldCheck` icons for the three nav items, `Menu`/`X` for the mobile hamburger toggle, and `LogOut` for the logout button; the active-state nav-link styling uses NavLink's `isActive` callback with brand-50 background and brand-700 text matching the AdminPanel tab styling. | An render error in `<AppHeader />` itself would be caught by the OUTER `<AppErrorBoundary>` and render the full-screen fallback (no chrome), losing the user's ability to navigate via header links during recovery; mitigated by the inner boundary wrapping `<Outlet />` so route render errors preserve the chrome above them — the more common failure mode. The header relies on `useSession()` and `useRole()` from the AuthProvider context, so missing the AuthProvider in main.tsx (a Checkpoint 6 implementation gap) would crash App with a clear `useContext` error inside `useAuthContext`'s missing-provider check; mitigated by AuthProvider being a Checkpoint 6 deliverable already present, by `frontend/tests/test-utils.tsx` using `MockAuthProvider` for tests, and by a future `tests/App.test.tsx` (Checkpoint 6) verifying the header rendering for both authenticated and unauthenticated cases. | 2026-05-04 | Accepted | — |
| DL-0042 | The `POST /api/connections` create endpoint includes a `Location: /api/connections/<id>` header on the HTTP 201 response per RFC 7231 Section 6.3.2. | Omit the Location header (the original Checkpoint 5 implementation);<br>return an absolute URL including scheme and host (`https://app.example.com/api/connections/<id>`);<br>return both a Location header AND the canonical URL in the JSON body. | RFC 7231 §6.3.2 specifies that 201 (Created) responses SHOULD include a Location header pointing at the newly created resource. The Checkpoint 5 review (CR-CKPT5-MINOR#1) explicitly flagged the missing header. Including it enables (a) HATEOAS-style client navigation without parsing the JSON body, (b) browser cache pre-population on the resource URL, and (c) symmetry with the standard REST shape that proxy intermediaries (CDN, WAF, ALB) recognize. The relative-path form (`/api/connections/<id>`) is preferred over an absolute URL because the SPA, dev environment, and three deploy environments (dev/staging/prod) all serve the API from different origins; relative paths are origin-agnostic per RFC 7231 §6.3.2 ("A Location field value can be either a URI-reference [absolute or relative]"). The single source-of-truth assertion lives at `tests/api/test_connections.py::TestPostCreate::test_contributor_creates_record_returns_201`. | A future change that splits the API onto a different sub-domain (e.g., `api.example.com`) would require changing the relative path to absolute; mitigated by the API-versioning rule (no `/v1/` prefix in MVP per AAP §0.6.2) and by the SPA and API shipping together so a domain split would be a coordinated migration that updates the Location header in lockstep. | 2026-05-04 | Accepted | — |
| DL-0043 | Split the database bootstrap into two Alembic migrations: `0001_initial_schema.py` (six tables, three enum types, seven indexes, and the `audit_events` GRANT/REVOKE block per AAP §0.4.7) and `0002_token_version_and_app_role.py` (adds `users.token_version` plus the idempotent `sales_connections_app` role provisioning that backs the GRANT/REVOKE in 0001). | Collapse both changes into a single `0001_initial_schema.py` migration matching the AAP §0.2.3 file inventory verbatim;<br>fold the role provisioning into a Terraform RDS resource so the role is created out-of-band before Alembic runs;<br>defer the `token_version` column to a future migration timed with a related session-management feature. | The AAP §0.7.4 invariant ("Tokens rotated on logout. Logout invalidates the cookie and (for the email/password flow) advances the per-user signing-key version.") requires a per-user counter the JWT verification middleware can compare against. The natural carrier is a `users.token_version` column read on every protected request. The AAP §0.7.1 invariant 5 ("Append-only audit table. Database-level grants enforce this in production.") requires that the application role exists at the moment the GRANT/REVOKE statements run, otherwise PostgreSQL silently no-ops the grants and the immutability invariant is unenforced at runtime. Both changes were discovered during Layer-1 backend implementation, AFTER 0001 had already been authored, reviewed, and merged. The two correct ways to address late-discovered schema needs in Alembic are (a) edit the existing migration in-place or (b) author a follow-up migration. Editing 0001 in-place would have rewritten history that other contributors had already applied locally and in CI, breaking the "Migrations forward-compatible" rule (AAP §0.7.7) and producing checksum drift between branches. Authoring 0002 preserves both invariants without any history rewrite. The 0002 migration is idempotent on the role provisioning (uses `DO $$ ... IF NOT EXISTS ... $$`) so repeated `alembic upgrade head` runs are safe. Folding role provisioning into Terraform was rejected because Alembic owns the schema lifecycle end-to-end (per the docker-compose dev stack design) and splitting responsibility across two tools would make local-dev setup require AWS credentials. The QA Checkpoint 3 report Issue #3 (INFO) flagged the deviation from the AAP file inventory but confirmed both changes are required by AAP invariants and that both upgrade and downgrade paths verify clean. | The AAP §0.2.3 file inventory now has a documented deviation; mitigated by this decision-log entry making the rationale discoverable from `docs/decision-log.md` (the canonical Explainability surface) and by both migrations being covered by `backend/tests/conftest.py` schema-bootstrap fixtures. A future contributor who reads only the AAP and wonders "why is there a 0002?" will find this row immediately. The role provisioning's `DO $$` block requires CREATEROLE or superuser on the migration role; if the connecting role lacks that privilege the block raises a clear NOTICE and continues, leaving the GRANT/REVOKE to apply against whatever role does exist - documented in the migration's docstring. | 2026-05-04 | Accepted | — |
| DL-0044 | Introduce a dedicated `app/middleware/security_headers.py` module that registers a Flask `after_request` hook to attach defensive HTTP response headers (`X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: geolocation=(), microphone=(), camera=()` on every response; `Cache-Control: no-store, no-cache, must-revalidate, private` on `/api/*` and `/auth/*` non-OPTIONS responses; `Strict-Transport-Security: max-age=31536000; includeSubDomains` only when `SESSION_COOKIE_SECURE=True`) on every backend response. The middleware is registered AFTER `register_cors_middleware` and BEFORE `register_auth_middleware` so the after-request chain runs correlation -> CORS -> security_headers and so the auth 401 short-circuit response also receives the headers. | Inline `@app.after_request` decorator inside `app/__init__.py` (the QA report's suggested fix);<br>add `flask-talisman` as a dependency for a battle-tested implementation;<br>set the headers at the AWS ALB layer via response-header rewrite rules instead of in application code;<br>set the headers in `app/middleware/error_handlers.py` so they only attach to error responses;<br>configure nginx as an in-task sidecar to interpose on the `/api/*` path and copy the frontend nginx's existing `add_header` directives. | The QA Checkpoint 7 Issue #1 (MEDIUM) flagged that production `/api/*` and `/auth/*` responses lacked the four defensive headers because the AWS ALB routes those paths directly to Flask without nginx interposition (AAP §0.4.6). The frontend nginx already sets the same headers via `add_header` directives in `frontend/nginx.conf:101-104`, but only on the SPA static-asset path it serves. A separate Python module mirrors the convention established by `correlation.py` and `cors.py` (one-module-per-responsibility middleware), keeps the registration path top-to-bottom readable in `app/__init__.py`, and makes the headers grep-able for future contributors. The inline decorator option was rejected because it would clutter the factory file and depart from the established middleware-module pattern. `flask-talisman` was rejected because adding a third-party dependency for a small, well-understood surface (six headers) duplicates the project's existing hand-rolled CORS module rationale (DL-0010 / `cors.py`) and would require pinning yet another package. ALB-layer header rewrite was rejected because (a) it would require Terraform-managed ALB listener rules that are environment-specific (the dev environment has no ALB; tests cannot exercise the headers; local docker-compose lacks an ALB) and (b) the AAP §0.7.4 security invariants ("the application is not complete until it is observable") require behavior verifiable at the application layer, not behind infrastructure that may or may not be present. The error-handler-only option was rejected because security headers must attach to 2xx successes too (every JSON response is a candidate for clickjacking via iframe). The nginx-sidecar option was rejected because it doubles the network hop count, increases per-task memory by approximately 40 MB for nginx, and reproduces the same `add_header` behavior the application can implement in less than 100 lines of Python. The middleware is idempotent (presence-checked attachment via `if name not in response.headers`) so handlers that explicitly set their own values are preserved, matching the `correlation.py` convention. HSTS is gated on `SESSION_COOKIE_SECURE` so dev/test environments running over plain HTTP do not lock browsers to HTTPS for localhost. The `Cache-Control` no-store directive is intentionally excluded from `/healthz`, `/readyz`, and `/metrics` because those endpoints carry no user data and their pollers benefit from short-window caching. | A future contributor who adds an endpoint that legitimately needs different cache semantics (e.g., a public org-tag list with `Cache-Control: public, max-age=3600`) must explicitly set `Cache-Control` in the route handler; mitigated by the idempotent attachment semantics and by the unit test `test_cache_control_preserved_when_handler_sets_it` that exercises this contract. The header set is opinionated; future product changes that legitimately require iframe embedding (post-MVP hosted-widget surface) must override `X-Frame-Options` per-route and update this decision-log entry; mitigated by the same idempotent semantics. The middleware adds one more hook to the after-request chain (now four hooks: correlation, CORS, security_headers, plus any blueprint-level hooks); per-request overhead is bounded by a constant-time iteration over the four-element static-headers dict plus two prefix checks, well under 1 microsecond per request — orders of magnitude below the 100-millisecond audit-emission budget (AAP §0.7.3). | 2026-05-04 | Accepted | — |
| DL-0045 | Enable PKCE (Proof Key for Code Exchange, RFC 7636) on the Google OAuth flow by setting `code_challenge_method: "S256"` in the Authlib client `client_kwargs` registered in `app/extensions.py::init_oauth_clients`; ALSO add `default_timeout: 10` to bound every outbound HTTP request the OAuth2 session issues to Google; ALSO enforce the OpenID Connect `email_verified` boolean claim inside `app/services/auth.py::upsert_oauth_user`, raising `ValueError` when the claim is anything other than `True` (or the JSON-decoded string `"true"`). | Rely on Authlib's defaults (no PKCE, no explicit timeout, no `email_verified` enforcement) — the original Checkpoint 8 implementation flagged by QA as MAJOR (PKCE missing) + MINOR (`email_verified` ignored) + INFO (no timeout);<br>enforce `email_verified` only at the API handler in `app/api/auth.py::google_callback` rather than at the service boundary;<br>set the OAuth timeout at the Gunicorn worker level (`--timeout 30`) and accept worker retirement as the sole resilience mechanism;<br>switch to `code_challenge_method: "plain"` (RFC 7636 allows it but is widely deprecated). | The QA Checkpoint 8 report identified three integration gaps in the Google OAuth flow: (1) the Authlib client registration omitted PKCE despite AAP §0.4.5 explicitly mandating "state and PKCE parameters" and the `google_start` docstring claiming PKCE was active — the URL emitted by `/auth/google/start` carried only `client_id`, `nonce`, `redirect_uri`, `response_type`, `scope`, and `state`, with `code_challenge` and `code_challenge_method` MISSING; (2) the `email_verified` claim from the validated ID token was ignored entirely, so an OAuth flow that surfaced an unverified email (rare for first-party Google accounts but possible for Workspace tenants with unverified domain aliases) would still upsert a User row; (3) the underlying `OAuth2Session` had no `default_timeout`, so a slow Google response would block the Gunicorn worker until its `--timeout 30` kill fires, retiring the worker for one full request. **PKCE (S256)**: setting `code_challenge_method: "S256"` causes Authlib's `OAuth2Session._create_oauth2_authorization_url` (sync_app.py:271-299) to auto-generate a per-flow `code_verifier`, compute `code_challenge = base64url(SHA256(code_verifier))`, and include both parameters in the redirect URL; the verifier is persisted in the framework session and replayed at `authorize_access_token`. This is the AAP §0.4.5 / OWASP-recommended defense against authorization-code-interception attacks: an attacker who intercepts the redirect-URL `code` parameter (e.g., via a malicious browser extension, a compromised proxy, or an open-redirect on the redirect_uri host) cannot exchange that code for tokens without the verifier, which never leaves the user's browser session. The `S256` method is preferred over `plain` per RFC 7636 §4.2 because the `plain` method offers no protection against attackers who can read the authorization request itself. **`email_verified` at the service boundary**: enforcing the check inside `upsert_oauth_user` provides defense-in-depth — even if a future caller forgets to validate (e.g., a CLI tool that imports `upsert_oauth_user` directly), the service still rejects unverified emails. The check is a simple boolean on the already-validated claims dict; latency is negligible. We accept both `True` and the string `"true"` because Authlib's claim deserialization does not normalize boolean strings universally. The existing service test `test_email_not_verified_raises_auth_error` (in `backend/tests/services/test_auth_service.py`) was authored defensively to accept either an `AuthError` raise OR a successful upsert — the new behavior takes the AuthError path cleanly. **`default_timeout`**: 10 seconds is comfortably above Google's typical sub-second OAuth response (token-exchange + JWKS fetch via discovery) while preventing pathological hangs that would saturate the Gunicorn worker pool. The timeout is per-request (Authlib forwards it to the underlying `requests.Session.request` via `kwargs.setdefault("timeout", self.default_timeout)` in oauth2_session.py:134-135), so each outbound call gets its own 10-second budget rather than a cumulative one. AAP §0.7.3 lists the authentication budget as 2 seconds end-to-end, but that includes the user's browser round-trip to Google — the explicit timeout caps only the server-side portion of the flow. The QA report's INFO observation noted that Authlib's default is `default_timeout=None` (no timeout); this fix closes that resilience gap. | A misconfigured Google OAuth tenant that issues `email_verified=false` for legitimate users (uncommon but possible for newly-provisioned Workspace accounts whose domain has not yet completed DNS verification) would fail to log in; mitigated because the rejection log line `oauth_user_email_not_verified` carries the email and the verification claim, so operators can diagnose the issue from CloudWatch logs and either (a) advise the user to complete Google's email verification, or (b) bypass via the email/password fallback flow if one exists. The PKCE addition is fully backward-compatible: Google's authorization endpoint accepts but does not require PKCE, so existing integrations continue to work; clients that previously cached pre-PKCE authorization URLs would simply re-issue the request after the redirect. The `default_timeout` of 10 seconds is well above Google's worst-case observed latency (P99 < 3 s in operational metrics from comparable Authlib deployments) but a future spike in Google's tail latency could surface as `requests.Timeout` exceptions caught by the existing `try/except Exception` in `google_callback`; mitigated because the existing exception handler converts these to AuthError → HTTP 401, which the SPA renders as "Sign-in failed" with a retry option. | 2026-05-04 | Accepted | — |
| DL-0046 | Introduce a dedicated `app/middleware/compression.py` module that registers the third-party `Flask-Compress==1.18` (pinned to `1.24` after PyPI version verification — the latest stable release at decision time) extension in the application factory at FIRST position in the middleware sequence (BEFORE `register_correlation_middleware`). The module configures `COMPRESS_ALGORITHM = ["gzip", "deflate"]` (deliberately excluding `br` brotli and `zstd` zstandard despite their codec wheels being installed transitively), `COMPRESS_MIN_SIZE = 500` (bytes), and `COMPRESS_LEVEL = 6` (canonical zlib default). Add `Flask-Compress==1.24`, `brotli==1.2.0`, and `backports.zstd==1.4.0` to `backend/requirements.txt`. | Rely entirely on AWS ALB-level compression by setting `routing.http.response.compress.enabled` on the listener (the QA report's secondary suggested fix);<br>add an inline `@app.after_request` decorator inside `app/__init__.py` that hand-rolls the gzip negotiation (avoids the third-party dependency at the cost of reimplementing well-tested logic);<br>install Flask-Compress but enable all four algorithms (`zstd`, `br`, `gzip`, `deflate`) per the library default;<br>use `flask-squeeze` instead of `Flask-Compress` (offers brotli-preferred negotiation and BREACH-padding mitigation but has a much smaller user base and shallower historical maintenance footprint);<br>place the registration LAST in the middleware sequence so it runs FIRST in the LIFO after_request chain (would force the body off the wire before correlation/cors/security_headers attach their headers, breaking the Vary contract). | The QA Checkpoint 9 review (Issue #1, MINOR) flagged that production `/api/*` and `/auth/*` responses lacked the `Content-Encoding: gzip` header because the AWS ALB routes those paths directly to Flask without nginx interposition (AAP §0.4.6). The frontend nginx already gzips SPA static assets via the `gzip on` block in `frontend/nginx.conf:58-68` but only handles the SPA static-asset path served by the separate frontend container; backend JSON responses ship uncompressed over the public Internet, costing roughly 70-90 percent of wire bytes on the ~19 KB feed payload at the 10K-record scale ceiling. **Application-layer compression over ALB-layer**: the ALB-only path was rejected for four reasons: (a) the local docker-compose dev environment has no ALB, so developers debugging perceived-performance issues locally would observe behavior different from production; (b) AAP §0.4.9 invariant "the application is not complete until it is observable" extends to network-efficiency behavior, which must be verifiable at the application layer rather than behind environment-specific infrastructure; (c) ALB compression has subtle caveats around minimum body size, content-type allowlists, and HTTP/2 frame interactions that vary by ALB feature flag; (d) keeping compression in application code makes the `Content-Encoding: gzip` outcome reproducible by every test runner and curl invocation. **Dedicated middleware module over inline decorator**: mirrors the convention established by DL-0044 (security headers as a module rather than an inline decorator) and matches the one-module-per-responsibility shape of the other middleware files (`correlation.py`, `cors.py`, `security_headers.py`, `auth.py`, `rbac.py`, `error_handlers.py`). The inline-decorator option was rejected because it would clutter the factory file and depart from the established middleware-module pattern; the hand-rolled implementation was rejected because Flask-Compress is widely audited (312k weekly downloads per Snyk) and reimplementing the gzip negotiation logic adds risk without value. **Flask-Compress over flask-squeeze**: Flask-Compress has the deeper history, the simpler API, and a configuration knob (`COMPRESS_ALGORITHM`) that lets us keep gzip authoritative without losing the right to enable brotli later; flask-squeeze prefers brotli by default which would conflict with the QA reproduction step that explicitly used `Accept-Encoding: gzip`. **gzip + deflate only**: brotli is universally supported in Chrome and Firefox but more CPU-intensive (could push the form-submit P95 over the AAP §0.7.3 budget under sustained load) and the frontend nginx config does NOT enable brotli either, so leaving brotli disabled keeps wire behavior consistent across the SPA static-asset path and the backend JSON path. zstd compression is excellent but browser support is still emerging (Chrome 123+, Firefox 126+ as of mid-2024) and not all corporate proxies handle it gracefully; enabling it before mass-market support is mature would risk a small fraction of clients receiving bodies they cannot decode. The brotli and backports.zstd codec wheels are still installed because Flask-Compress imports them unconditionally at module load (the `try: import brotlicffi as brotli` block at the top of `flask_compress.flask_compress` and the `from backports import zstd` import in `flask_compress.compat`); restricting `COMPRESS_ALGORITHM` to gzip and deflate keeps the wire behavior simple even though the codecs sit unused in the worker process. **FIRST-position registration**: Flask runs `after_request` hooks in REVERSE registration order (LIFO) per the empirical contract verified by `tests/middleware/test_compression.py::TestRegistrationOrder::test_after_request_runs_in_reverse_registration_order`. Registering compression FIRST therefore runs it LAST in the chain, which is correct because the Flask-Compress hook reads the FINAL response body and Content-Length, computes the compressed body, then rewrites those values plus the Vary and Content-Encoding headers. Any other after_request hook that runs AFTER compression would see the compressed body, which is opaque, and would have no useful way to interact with it. Running upstream hooks first lets correlation/cors/security_headers finish all header attachment before compression takes the body off the wire. **Pinned versions per DL-0022**: Flask-Compress 1.24 was selected (latest stable on PyPI as of decision date; the to-do list draft mentioned 1.18 but version verification revealed 1.24 was current with cleaner direct `backports.zstd` dependency rather than the `pyzstd → backports.zstd` indirection in 1.18). `brotli==1.2.0` and `backports.zstd==1.4.0` are pinned as transitive deps per the existing pinning policy. | Flask-Compress imports `brotli` and `backports.zstd` at module load even when those algorithms are disabled in `COMPRESS_ALGORITHM`; the dependencies cannot be removed even though the codecs are not exercised. Mitigated by the wheels being small (brotli ~700 KB, backports.zstd ~1 MB) and well-maintained, and by Dependabot tracking them. A future endpoint that wants brotli (e.g., a public docs surface served from the backend) must update `COMPRESS_ALGORITHM` AND benchmark against the AAP §0.7.3 form-submit budget; the configuration knob is exposed via Flask config so a future change does not require a code edit. The Vary: Accept-Encoding header is correctly emitted by Flask-Compress on every response (verified by `TestVaryHeader`), but a future CDN deployed in front of the backend (post-MVP CloudFront for the API path) must respect Vary or risk serving gzipped bodies to clients that did not request gzip; this is documented in `docs/operations.md` deployment notes. The compression overhead per request is well under the 100 ms audit-emission budget at gzip level 6 on JSON payloads up to ~50 KB — measured locally the P95 add is under 2 ms — but a future pathologically-large response (e.g., admin export of all records) could push the latency budget; mitigated because no MVP endpoint emits more than ~25 KB and any future endpoint over that size should be paginated rather than served as a single response. The QA Checkpoint 9 report flagged ALB-level compression as an alternative; if a future deployment adds CloudFront in front of the ALB, the operators may choose to disable application-layer compression at that point — the `COMPRESS_REGISTER` config knob lets a future ProductionConfig override turn off the registration without changing this module. | 2026-05-04 | Accepted | — |
| DL-0047 | Use `structlog==24.4.0` as the structured-logging library; integrate stdlib `logging` records into the same processor chain via `structlog.stdlib.ProcessorFormatter` so SQLAlchemy / Authlib / Werkzeug / boto3 / urllib3 records render as the same JSON shape. | stdlib `logging` + `python-json-logger==2.0.x` (a JSON formatter for stdlib);<br>raw stdlib `logging` with a hand-rolled `Formatter` subclass emitting JSON;<br>`loguru==0.7.x` for a more opinionated API;<br>`google-cloud-logging` (vendor-bound to GCP). | structlog has first-class `contextvars` integration which lets the correlation-ID middleware bind a request-scoped UUID once and have every subsequent log line in the request automatically include it without per-call kwargs; stdlib `logging` requires a `LoggerAdapter` per request to achieve the same effect, which is significantly more invasive at the call sites. structlog's processor chain composition (a list of pure functions applied in order before the renderer) is exactly the right abstraction for the project's redaction-then-render-then-handoff pipeline (per DL-0047 redaction processor + the OpenTelemetry context processor + the JSON renderer). The `ProcessorFormatter` integration with stdlib `logging` lets us keep ONE renderer for both native structlog records (from application code) AND foreign stdlib records (from third-party libraries) so JSON shape is uniform across the process. python-json-logger lacks the contextvars story; loguru has its own opinionated handler model that conflicts with the stdlib `logging` bridge required by SQLAlchemy / Werkzeug; the GCP-bound option is a non-starter for an AWS deployment per AAP §0.4.6. The PyPI surface is mature (40M+ monthly downloads) and the API has been stable since 22.x. | structlog's API is less familiar to engineers coming from a stdlib-`logging`-centric Python background; mitigated by a glossary entry in `docs/onboarding.md` and by the conventions encoded in `app/observability/logging.py` (one canonical processor chain, idempotent `configure_structlog()`, stdlib bridge via `ProcessorFormatter`). The processor chain has a fixed order (filter → contextvars merge → logger name → log level → timestamp → stdlib-extras → OTel context → stack info → exc info → unicode decode → redaction → renderer); reordering a step requires updating both the chain and any downstream processor that depends on the prior step's output. | 2026-05-04 | Accepted | — |
| DL-0048 | Use `opentelemetry-sdk==1.29.0` + `opentelemetry-instrumentation-{flask,sqlalchemy,httpx,requests}==0.50b0` for distributed tracing, with the global `TracerProvider` installed at process startup by `app.observability.tracing.init_tracing`. | OpenCensus (deprecated in favor of OpenTelemetry by Google);<br>Jaeger client (vendor-specific, replaced by the OTel project as the canonical CNCF tracing API);<br>Datadog `dd-trace` (vendor-specific, would couple the application to Datadog's collector);<br>AWS X-Ray SDK (vendor-specific, less portable than OTLP);<br>no tracing at all (would violate the AAP §0.7.5 Observability rule). | OpenTelemetry is the CNCF-graduated, vendor-neutral standard for distributed tracing and the only choice that satisfies the user's "scalability in mind" intent (AAP §0.1.1) by keeping the deployment portable across observability backends (Jaeger locally, AWS X-Ray / Honeycomb / Datadog / Grafana Tempo in production) without code changes. The `OTLP HTTP/protobuf` transport works through standard ALB / proxy infrastructure without HTTP/2 or gRPC special-handling (per DL-0049). The four pinned auto-instrumentations cover every span boundary the MVP crosses: `Flask` for inbound requests, `SQLAlchemy` for database round-trips, `httpx` for the Anthropic Claude SDK's outbound calls, and `requests` for Authlib's Google OAuth code-exchange / JWKS-fetch calls. The `BatchSpanProcessor` (rather than `SimpleSpanProcessor`) batches spans on a background thread so per-request latency is not affected by exporter wire time. The `0.50b0` version pin is intentional: it matches the OTel SDK 1.29.0 release line and is the latest stable instrumentations version that supports both Python 3.12 (the project's pinned runtime per AAP §0.3.1) AND Flask 3.1.3 (per DL-0001). Vendor-specific alternatives were rejected because they would lock the project into one observability vendor at deploy time; the OpenTelemetry collector pattern (deploy a collector that forwards to whichever vendor the operator chooses) keeps that decision deferred to operations. | OpenTelemetry instrumentation versions are pinned to a beta-tagged release (`==0.50b0`); mitigated because the SDK side (`==1.29.0`) is GA and the instrumentations have been in beta tagging for years without breaking-change migrations between minor versions. The project's Dependabot config (DL-0026) auto-PRs upgrades for review. The auto-instrumentations have minor performance overhead per request (microseconds for span creation) which is well under any AAP §0.7.3 budget; verified locally via the existing `http_request_duration_seconds` histogram. A future internal downstream service must accept the `traceparent` / `tracestate` headers for end-to-end trace linkage; this is documented in `docs/operations.md` and is a post-MVP concern (no internal services exist yet). | 2026-05-04 | Accepted | — |
| DL-0049 | Use a custom `prometheus_client.CollectorRegistry` instance (`metrics_registry` in `app/extensions.py`) for the application's `/metrics` endpoint rather than the default global `prometheus_client.REGISTRY`. Built-in `ProcessCollector`, `PlatformCollector`, and `GCCollector` are explicitly registered against the custom registry to surface `process_*` and `python_*` metrics. | Use the default global `prometheus_client.REGISTRY` (the library's out-of-the-box default; auto-includes process/platform/gc collectors but cannot be re-initialized cleanly across pytest sessions);<br>use `prometheus-flask-exporter` (a third-party Flask extension that wraps the global REGISTRY with route-decorator helpers; adds a dependency the project does not need given its hand-rolled middleware);<br>use `prometheus_client.multiprocess.MultiProcessCollector` against a shared filesystem directory (the canonical pattern for multi-worker Gunicorn but introduces filesystem coordination complexity not warranted at MVP scale). | The default global `prometheus_client.REGISTRY` is a singleton; pytest-flask constructs multiple Flask apps per session and each `Counter("http_requests_total", ...)` registration would raise `ValueError: Duplicated timeseries` on the second app's import. A dedicated `CollectorRegistry` sidesteps this entirely and is also documented as the recommended pattern for libraries that want test isolation. The trade-off is that the library's built-in `ProcessCollector` / `PlatformCollector` / `GCCollector` auto-register against the GLOBAL registry only at module import time and therefore do NOT show up at the custom-registry `/metrics` endpoint. Per QA Checkpoint 10 Issue 5, this gap was MAJOR for Prometheus-native operator tooling (Grafana, Alertmanager, kube-prometheus-stack) which expect `process_resident_memory_bytes`, `process_open_fds`, `process_cpu_seconds_total`, and the `python_*` namespace at minimum. The fix is to construct each built-in collector explicitly with `registry=metrics_registry`; this is one line of code per collector in `app/extensions.py` and adds zero runtime overhead beyond what the global-REGISTRY default would have produced. The `multiprocess` mode was rejected for MVP because Gunicorn pre-fork workers each maintain independent counters and the project's CloudWatch dashboards aggregate at the ECS-task level; introducing a shared filesystem directory for inter-worker counter aggregation would add complexity (cleanup hooks, filesystem permissions in the container) without a corresponding operational benefit at the 10K-record scale ceiling. The `prometheus-flask-exporter` package was rejected because the project's Flask middleware already implements the same per-request counter / histogram instrumentation in `app/observability/metrics.py::_after_request_record_metrics` and adding a third-party wrapper would duplicate the surface. | A future contributor adding a new metric must remember to pass `registry=metrics_registry`; mitigated by the existing module-level `__all__` listing every metric instrument and by the test fixture pattern that exercises every metric in `tests/observability/test_metrics_endpoint.py`. The custom registry does not benefit from `prometheus_client`'s built-in `multiprocess` mode automatically; if a future Gunicorn worker count grows beyond the point where per-worker counter aggregation in CloudWatch is sufficient, the migration to multiprocess mode requires a one-time switch to `MultiProcessCollector`, which is documented as a post-MVP suggested next task in `docs/onboarding.md`. The newly-added built-in collectors (`process_*`, `python_*`) increase the `/metrics` endpoint payload size by approximately 1.5 KB; this is well within Prometheus' practical scrape size limits and the increase is a one-time fixed cost rather than a per-request cost. | 2026-05-04 | Accepted | — |
| DL-0050 | Use the OTLP HTTP/protobuf transport (`opentelemetry-exporter-otlp-proto-http`) for the OpenTelemetry span exporter, NOT the gRPC variant (`opentelemetry-exporter-otlp-proto-grpc`). | OTLP gRPC over HTTP/2 (the more "default" OTel exporter when both options are documented);<br>Jaeger Thrift over UDP (the legacy Jaeger native transport, deprecated by the OTel project);<br>Zipkin HTTP/JSON (an older alternative that predates the OTLP standard);<br>build a custom HTTP exporter (zero ecosystem support, would have to be maintained in-house). | HTTP/protobuf works through standard ALB / proxy infrastructure without the HTTP/2 special-handling (`grpc.lb_policy`, `keepalive_time_ms`, header-frame translation) that gRPC requires; the AWS Application Load Balancer DOES support gRPC but ONLY over HTTPS and ONLY with explicit configuration, whereas HTTP/protobuf over HTTPS works out of the box. The `OTLP HTTP/protobuf` transport's default endpoint shape (`http://localhost:4318/v1/traces`) is identical for local Jaeger, the OpenTelemetry Collector, and most managed-vendor backends (Honeycomb, Grafana Tempo, AWS Distro for OpenTelemetry collector), simplifying the operator-facing configuration story to a single env var (`OTLP_EXPORTER_ENDPOINT`). gRPC's persistent-connection model also complicates auto-scaling and connection pooling under bursty traffic: a fresh gRPC channel takes 2-3 RTT to establish, whereas HTTP keep-alive pooling reuses connections trivially. The protobuf wire format (gzipped over the wire by default) is the same on both transports; the choice is purely transport-level, not encoding-level, so no payload-size or fidelity trade-off is incurred. The Jaeger Thrift / Zipkin alternatives are rejected because they predate the OTLP standard and would lock the project into one collector vendor; the OTLP standard is the explicit replacement for both. | A future deployment that requires gRPC (e.g., a self-hosted Tempo instance configured for gRPC ingest only) must swap `opentelemetry-exporter-otlp-proto-http` for `opentelemetry-exporter-otlp-proto-grpc` and update `app/observability/tracing.py::_build_exporter` to use `OTLPSpanExporter` from the gRPC sub-package; the swap is one line of change and is documented inline. The HTTP/protobuf transport has a slightly higher per-request overhead than gRPC at sustained high-volume span emission (millions of spans/second) due to HTTP/1.1 framing; this is irrelevant at the MVP scale ceiling (10K records per organization yields O(10) spans/second steady-state) and the `BatchSpanProcessor` further amortizes the overhead by batching spans on a background thread. | 2026-05-04 | Accepted | — |
| DL-0051 | Sign the F-012 session JWT with the HMAC-SHA-256 (`HS256`) symmetric algorithm, not the RSA-SHA-256 (`RS256`) asymmetric algorithm. The signing key (`JWT_SIGNING_KEY`) is a single shared HMAC secret stored in AWS Secrets Manager; the same key is used to mint and to verify session tokens. | RS256 with an asymmetric private key for minting and a public key for verification (the canonical asymmetric JWT pattern when verifiers and minters live in separate trust boundaries);<br>EdDSA (`Ed25519`) for a smaller signature footprint than RS256;<br>JWE (encrypted JWT) for confidentiality of the claim payload in addition to integrity. | The Sales-Connections backend is the SOLE authority that both mints and verifies session JWTs. There is exactly one verifying party (the Flask process inside the same container that minted the token) so the symmetric model (`HS256`) is operationally simpler than asymmetric: one secret to rotate, one config knob, no public-key distribution machinery, no JWKS endpoint. RS256 is the right choice when verifiers must trust a token without holding the signing material (e.g., third-party API consumers verifying an auth-server-issued token via JWKS) — that is not the architecture here. EdDSA was rejected because PyJWT's EdDSA path requires `cryptography` with the `Ed25519` key support, adding a transitive dependency for no operational benefit at this scale. JWE was rejected because the JWT claims are already non-sensitive (user_id, org_id, role, email, display_name, tv, iat, exp) — none of these warrant payload encryption — and JWE doubles the per-request crypto cost. PyJWT's `HS256` validation is implemented in pure C via the bundled `hmac` module and runs in tens of microseconds per request, well under the AAP §0.7.3 sub-50 ms RBAC budget. The HS256 signing-key length is 256 bits per RFC 7518 §3.2; the project enforces a minimum byte-length (32 bytes for HS256) when the key is loaded. | A future architecture in which a downstream service must verify the JWT without holding the signing material (e.g., a separate analytics service) would require migrating to RS256 and standing up a JWKS endpoint; mitigated because the MVP architecture has no such verifier and the migration is a one-time well-understood path. The HMAC secret must never leak — its compromise lets an attacker mint arbitrary tokens — and the project mitigates this with AWS Secrets Manager IAM-bounded retrieval, structlog redaction (DL-0047 redaction processor), and the per-user `users.token_version` mechanism (DL-0043) that lets us invalidate every active session in one SQL UPDATE if the secret is suspected to have leaked. The choice was AAP-mandated by §0.7.4 ("Tokens minted via PyJWT") combined with the absence of any AAP-mandated requirement for asymmetric verification — this row exists per the QA Checkpoint Final Documentation Review I-01 recommendation that AAP-mandated architectural choices receive an explicit Decision Log entry so future contributors can revisit the trade-off without reverse-engineering the AAP. | 2026-05-04 | Accepted | — |
| DL-0052 | Build the SPA on React 19.2.5 + TypeScript 5.x + Vite 5.x; do NOT use Next.js, Remix, Gatsby, Astro, or any meta-framework. | Next.js 15 with App Router (Vercel's React meta-framework offering server-rendering, SSG, ISR, and React Server Components);<br>Remix 2 (React Router team's full-stack meta-framework with nested routing and form actions);<br>Gatsby 5 (build-time static-site generator);<br>Astro 4 (multi-framework static-first compiler);<br>Create React App (deprecated by the React team in 2023). | The SPA delivery surface (DL-0017 single-`index.html` Vite bundle) requires zero server-rendering, zero static-generation, zero edge-rendering — every authenticated route is dynamic and gated by the JWT-bearing cookie, so SSR/SSG would offer no SEO or perceived-performance benefit. Next.js' App Router and React Server Components add a server-side rendering substrate that the AAP does NOT call for and that introduces a deployment topology incompatible with the chosen "static SPA + Flask API" split (DL-0023): RSC requires a Node.js runtime as a peer to the Python API, doubling the runtime surface and creating an unowned auth boundary between the Node SSR layer and the Flask backend. Remix has the same architectural cost (full Node runtime). Gatsby is build-time-only, which is fundamentally incompatible with a dynamic authenticated app. Astro is a multi-framework compiler whose SPA story is weaker than Vite's. CRA is officially deprecated. Vite + React-Router is the canonical SPA toolchain in 2026 and matches the AAP §3.2 stack mandate verbatim ("React 19.2.5 + TypeScript 5.x + Vite"). The choice was AAP-mandated by §3.2; this row exists per the I-01 recommendation. | A future requirement for SEO on a public-facing marketing surface would require either (a) standing up a separate Next.js/Astro app for the marketing site only, with the SPA continuing to serve the authenticated app surface, or (b) introducing Vite SSR via `vite-plugin-ssr` (now `vike`) to add render-on-demand to the existing toolchain. Mitigated because the MVP has no public-facing surface beyond the login screen, and the marketing-site question is firmly post-MVP per AAP §0.6.2. The decision is straightforward to revisit if SEO surfaces become a requirement. | 2026-05-04 | Accepted | — |
| DL-0053 | Run the backend container on AWS ECS Fargate, NOT on AWS EKS, AWS App Runner, AWS Lambda, AWS Beanstalk, or AWS EC2 Auto Scaling Groups. | EKS (Elastic Kubernetes Service) for declarative Kubernetes manifests and the broader CNCF ecosystem;<br>App Runner (the lightest-weight AWS container service, abstracts ECS entirely);<br>Lambda with the container-image runtime (event-driven, cold-start trade-offs);<br>Beanstalk (legacy AWS PaaS, EC2-based);<br>EC2 ASG with Docker installed (lowest-level, most operator burden). | ECS Fargate is the right fit for the Sales-Connections workload at MVP scale: the application is a single long-running stateless container per task (DL-0001 stateless backend invariant), traffic is steady-state HTTP not event-driven, and there is no Kubernetes-specific feature being consumed (no CRDs, no operators, no custom controllers, no service mesh). EKS would add a control-plane cost (~$73/month per cluster) and the operational burden of cluster upgrades, node-group management, kubectl tooling, and Helm chart authoring — costs that are unjustified at the 10K-record scale ceiling and would violate the AAP §0.7.7 "two-Docker-image release strategy" simplicity goal. App Runner abstracts ECS too aggressively for this project — it does not expose the ALB integration or task-role granularity that the AAP §0.4.6 four-integration-surface design requires. Lambda was rejected because the cold-start path conflicts with the AAP §0.7.3 form-submit ≤2 s P95 budget (Lambda cold start on a Python 3.12 image with the full Flask + SQLAlchemy + Anthropic SDK dependency tree is ~3-5 s on the first invocation per worker). Beanstalk is legacy and EC2-based (not container-native). EC2 ASG with Docker installed dumps every operational concern (patching, scaling logic, log routing) on the operator. ECS Fargate gives serverless containers (no instance management), per-task IAM roles (the deploy-role/task-role split per AAP §0.7.4), native ALB target-group integration, native CloudWatch logging via the `awslogs` driver, and stable cold-start (~30 s task placement, ZERO cold start per request once the task is RUNNING). The choice was AAP-mandated by §3.6 ("AWS ECS Fargate"); this row exists per the I-01 recommendation. | A future architecture that grows to dozens of services with shared infrastructure (e.g., a service mesh, a shared cache, a job-queue runner) may benefit from EKS for its richer service-graph primitives; mitigated because the MVP has exactly one service and the post-MVP roadmap (AAP §0.6.2) does not call for additional services. The Fargate cost per task-vCPU-hour is ~25% higher than equivalent EC2-on-Spot pricing; mitigated because at the 10K-record scale ceiling the project runs 2-4 tasks total and the absolute monthly cost differential is under $50. A future audit of $/request-served may re-open this decision in favor of EKS-on-Spot for cost reduction; the migration path from ECS Fargate to EKS is one-time and well-documented. | 2026-05-04 | Accepted | — |
| DL-0054 | The `.github/workflows/cd.yml` deploy pipeline authenticates to AWS via OIDC federation (`aws-actions/configure-aws-credentials@v4` with `role-to-assume`); long-lived AWS access keys (`AWS_ACCESS_KEY_ID` + `AWS_SECRET_ACCESS_KEY`) are NOT stored as GitHub Actions secrets. | Long-lived AWS access keys stored as GitHub Actions secrets (the pre-2022 canonical pattern);<br>self-hosted GitHub Actions runners with EC2 instance roles (the on-prem variant);<br>AWS CodePipeline as the deploy orchestrator (CodeCommit-native, abandons GitHub-as-orchestrator);<br>third-party deploy tools (CircleCI / Buildkite / Jenkins) with their own AWS auth story. | OIDC federation is the modern AWS-recommended pattern for CI/CD: GitHub's OIDC issuer mints a short-lived ID token per workflow run, AWS's IAM federation exchanges it for a 1-hour STS session credential keyed to the deploy role's session policy. The credential lifetime is bounded; the credential is never written to disk; the credential cannot be exfiltrated to a long-term store; the credential is keyed to a specific repository and a specific branch (or environment) via the IAM role's trust policy. Long-lived access keys violate the AAP §0.7.4 invariant ("Tokens never exposed to the client") in spirit — they are unbounded credentials sitting in the GitHub secrets store, recoverable by anyone who compromises the GitHub org. Self-hosted runners are operationally heavy (you become responsible for runner patching, scaling, network isolation) and add a long-lived AWS surface that OIDC obviates. CodePipeline is GitHub-incompatible at the trigger level (CodeCommit-native) and would force a mirror-and-trigger setup that doubles the source-of-truth surface. Third-party tools each have their own auth story but share the same shortcoming as static-key GitHub Actions. The choice was AAP-mandated by §3.6 ("OIDC-federated GitHub Actions deploy role") and §0.7.4 ("OIDC-federated GitHub Actions assumes a deploy role without long-lived AWS keys"); this row exists per the I-01 recommendation. | A future CI/CD migration to a different orchestrator (e.g., GitLab CI on a GitLab Premium tenant) requires re-authoring the OIDC trust policy to admit the new IDP's issuer URL — the IAM trust-policy change is a one-line edit and is documented in `docs/operations.md` deployment notes. The OIDC token's JWT claim shape is GitHub-specific (`repo`, `ref`, `environment`, `actor`, `workflow`); a different CI orchestrator will use a different claim shape and the IAM trust policy's `Condition` block must be updated accordingly. The current trust policy gates the deploy role on `token.actions.githubusercontent.com:sub` matching `repo:Sales-Connections/sales-connections:ref:refs/heads/main` (or `:environment:production`), which is the strongest cross-repo / cross-branch isolation OIDC offers. | 2026-05-04 | Accepted | — |
| DL-0055 | Use PostgreSQL `JSONB` (binary JSON) for the `audit_events.before_payload` and `audit_events.after_payload` columns, not `TEXT`-encoded JSON, not `JSON` (the textual variant), not separate normalized tables. | Plain `TEXT` columns containing application-serialized JSON (the PostgreSQL-7.x-era pattern);<br>the older `JSON` PostgreSQL type (textual JSON with parse-once-on-cast semantics);<br>fully normalized audit tables (one row per (record_id, field_name, before_value, after_value));<br>append-only event sourcing in a separate event-log database. | `JSONB` is the right type for the audit-payload columns at three levels. (1) Storage: `JSONB` is binary-encoded with deduplicated keys and stored on heap-with-toast — the same payload as `TEXT`-JSON costs ~30% less disk. (2) Query: `JSONB` supports the `@>`, `?`, `?&`, `?|` containment / existence operators and the `jsonb_path_query`, `jsonb_each` table-functions, which the F-014 admin moderation surface uses to filter audit events by changed-field name without parsing the JSON application-side. (3) Index: `JSONB` supports `GIN` indexes (one index over all keys, or expression indexes over specific JSON paths) — the schema does NOT add such indexes today (none are needed at the 10K-record scale ceiling) but the option is open for post-MVP without a column-type migration. `TEXT`-JSON has none of these properties and would force every audit query to deserialize on the application side. The older `JSON` type (textual) has the operators but not the storage compression and parses-on-every-cast which is wasteful. Fully normalized audit tables (one row per changed field) were rejected because the audit payload shape varies per event_type (a `status_change` has 1 changed field; an `edit` may have 5; an `authentication` has 0 — only a session id) and modeling that variability with a relational schema would either (a) bloat the schema with sparse columns or (b) introduce an EAV anti-pattern. The single-`JSONB`-blob-per-event approach is the Stripe / Shopify / GitHub Audit Log pattern and is the canonical solution. Event sourcing in a separate event-log database was rejected as massive over-engineering for the MVP audit surface (AAP §0.6.2 explicitly excludes event-sourcing). The choice was AAP-mandated by §6.2 ("audit_events table with before_payload jsonb, after_payload jsonb"); this row exists per the I-01 recommendation. | A future requirement to constrain the JSON shape of `before_payload` / `after_payload` (e.g., enforce that an `edit` event always contains a `field` key) is achievable via PostgreSQL CHECK constraints with `jsonb_path_exists` predicates — possible at the database level but not enforced in MVP, mitigated because pydantic schemas at the audit-emitter layer (`backend/app/services/audit.py`) enforce the shape before INSERT. The `JSONB` type loses the original key ordering of the input JSON; mitigated because the audit consumer never depends on key ordering (the diff renderer in the F-011 EditHistoryFeed sorts keys for display). | 2026-05-04 | Accepted | — |
| DL-0056 | Pin the Trivy GitHub Action used in `.github/workflows/ci.yml` security-scan job to the specific tagged release `aquasecurity/trivy-action@0.28.0` rather than `@latest`, `@v0`, or any floating major-version tag; pin it consistently across all four Trivy invocations in the workflow (backend SARIF, frontend SARIF, backend table-fail, frontend table-fail). | Pin to `@latest` (the literal floating tag);<br>pin to `@v0` or `@v0.x` (a moving major-version tag);<br>pin to a commit SHA via `@<sha>` for the strongest immutability (the OWASP-recommended pattern for actions);<br>install the Trivy CLI directly in a `run:` step and skip the official action entirely. | The AAP Sec 0.7.7 "Pinned versions everywhere" rule applies to GitHub Action references with the same force as it does to pip and npm dependencies: floating tags (`@latest`, `@v0`) introduce non-determinism into CI runs because the upstream tag can be re-pointed at any time, and a security-scanner action specifically is positioned to silently change scan behavior in ways that mask real vulnerabilities (e.g., a tag re-point that introduces a regression in CVE detection would flip the build green for issues that were previously red). `0.28.0` was the current stable release of `aquasecurity/trivy-action` at the time of CI authoring; pinning the exact tag preserves CI reproducibility across PR runs and lets Dependabot's `github-actions` ecosystem (DL-0026, .github/dependabot.yml) auto-PR known-good upgrades through the standard review path. The commit-SHA pin (the OWASP-hardened variant) was rejected for now because: (a) the four-call surface area in ci.yml would need four identical SHA strings and any rotation would require updating all four; (b) Dependabot's GitHub Actions update flow handles tag pins correctly but emits less-useful PR titles for SHA pins; (c) the `0.28.0` tag is signed and verified by the publisher, providing a tractable trust anchor without the SHA-pin maintenance burden. The CLI-only path was rejected because the official action wraps SARIF-output handling, GitHub Security tab integration, and exit-code semantics that re-implementing in shell would be error-prone. The QA Final Checkpoint 13 review (Issue #6, MINOR) flagged that this rationale was implicit but not captured in the decision log; this entry closes the Explainability gap per AAP Sec 0.7.5 / DL-0023 (the decision log is the canonical source-of-truth for "why" decisions). | A pinned action tag eventually goes stale; mitigated by Dependabot's weekly Monday batch (DL-0026, schedule.day = monday) auto-PRing newer versions for review. The PR-review checklist in `.github/pull_request_template.md` requires a decision-log entry whenever a security-relevant version pin moves, so a future Trivy bump (e.g., 0.28.0 -> 0.29.0) will produce a corresponding decision-log row updating the rationale rather than silently superseding this entry. If Dependabot ever loses access to the action's release feed (e.g., the publisher renames the repository), the workflow will continue to work against the pinned tag indefinitely — the cost is staleness, not breakage. | 2026-05-05 | Accepted | — |
| DL-0057 | Replace every reference to the deprecated `data.aws_region.current.name` attribute with `data.aws_region.current.region` across all Terraform modules (`network/vpc_endpoints.tf`, `ecs/data.tf`, `ecs/locals.tf`, and the top-level `main.tf`), and document the migration before AWS provider 6.x adoption. | Leave the `.name` references as-is and tolerate the per-validate deprecation warning until the AWS provider 6.x upgrade forces the migration;<br>add a `# noqa` style suppression marker to each line referencing `.name` (Terraform does not support per-line warning suppressions);<br>encapsulate the region attribute lookup behind a single `local.region` and reference the local everywhere (would have required edits to roughly the same line set as the direct `.region` substitution but adds an indirection layer with no operational benefit). | The AWS provider's `aws_region` data source deprecated the `.name` attribute in favor of `.region` (issue hashicorp/terraform-provider-aws#42468; the deprecation lands in 5.x and the attribute will be removed entirely in the 6.x major release per the provider's stated deprecation policy). Validating each module standalone produces six deprecation warnings (`vpc_endpoints.tf:109,134,157,181,205,229` for the network module; `ecs/data.tf:203,204,313` and `ecs/locals.tf:146,198,328` for the ecs module). The `.region` attribute is available in AWS provider 5.79.0+ AND in 6.x — making the migration both backward- and forward-compatible. The QA Final Checkpoint 13 review (Issue #2, INFO) flagged the deprecation as non-blocking technical debt that must be addressed before the AWS provider 6.x upgrade; the fix removes all six warnings without changing wire behavior (the same region string is resolved either way). The decision to fix the deprecation NOW (rather than batching it with the eventual 5.x -> 6.x upgrade) follows from AAP Sec 0.7.7 "Lint must pass" — `terraform validate` warnings are the IaC equivalent of lint warnings and the project's "zero warnings in CI" posture demands they be cleared at root cause rather than tolerated. The single-`local.region`-indirection alternative was rejected because it would either (a) require a `locals` block in every module that references the data source, or (b) require threading a new `region` input variable through every module — both options trade indirection for no benefit. The direct attribute substitution preserves the "data source local to the module" pattern already established and produces a one-line diff per call site. | A future contributor copying boilerplate from older Terraform examples (where `.name` was the canonical attribute) may reintroduce the deprecation; mitigated by `terraform validate -json` running in `terraform validate` CI which surfaces deprecation warnings and by the Dependabot-driven AWS provider upgrade flow (DL-0026 ecosystem entry) which would surface a concrete failure when the attribute is removed in 6.x. The migration is reversible via a search-and-replace in the unlikely scenario that an older AWS provider <5.79 must be reinstated; mitigated because the project pins the provider at `~> 5.70` (versions.tf) and 5.79+ is comfortably within range. | 2026-05-05 | Superseded | DL-0059 |
| DL-0058 | Accept the frontend container image's measured runtime size of approximately 54.5 MB (vs the QA Final Checkpoint 13 stretch target of <50 MB) as production-ready; document the size rationale in this decision log instead of further reducing the image. | Switch the runtime base from `nginx:1.27-alpine` (~48.2 MB) to `nginx:alpine-slim` (smaller variant) — would require investigation into whether the slim variant ships the modules used by `frontend/nginx.conf` (gzip, brotli, security headers, history-mode fallback);<br>switch to `gcr.io/distroless/nginx` (smaller and rootless) — adds a less-common base image that operators must learn;<br>build the SPA bundle into a `scratch` image that is served by a tiny Go binary (e.g., `caddy` static-only) — replaces the nginx surface with a less-battle-tested static server;<br>squash the layers into a single image to remove inter-layer overhead (Docker layer hashing already dedupes shared content; squashing removes the value of layer caching and is widely discouraged). | The 54.5 MB total decomposes as: base image `nginx:1.27-alpine` ~48.2 MB; built SPA bundle copied via `COPY` ~3.0 MB; nginx.conf and miscellaneous static assets ~0.1 MB; Docker layer overhead ~3.2 MB. The base image alone consumes ~88% of the total — the absolute floor reachable without changing the runtime base. The SPA bundle is already at its production-optimized size (Vite tree-shaking, Tailwind purge, content-hash assets). The QA "<50 MB" target was a stretch goal documented in the checkpoint instructions; the AAP-mandated target in `frontend/Dockerfile`'s preamble ("well under 60 MB") IS met. Switching to `nginx:alpine-slim` was investigated as a follow-up but is non-trivial: the slim variant strips modules that the project's nginx.conf actually uses (e.g., the gzip module is built-in to the standard alpine image but historically optional in slim variants). Switching to `distroless/nginx` is feasible but introduces an operator learning cost (no shell, different debugging story) for a ~2 MB savings. The Caddy/static-binary path replaces a battle-tested HTTP/2 web server with a less-mature replacement. The QA Final Checkpoint 13 review (Issue #1, INFO) explicitly suggested either reducing the image or documenting the deviation; this decision-log entry is the documented deviation. | A future container hardening effort that warrants a second pass (e.g., a security audit, a CDN cost optimization) should re-examine the slim/distroless options with concrete benchmarks: install size, cold-start time, image-pull bandwidth across 1000 deploys/month, and feature parity with the project's nginx.conf. The documented size cost (~6.5 MB above the stretch target) is bounded; mitigated because ECR storage cost at this size is negligible (~$0.10/GB/month for a single image revision), ECS cold-start time is dominated by ECR pull latency which is independent of image size at this scale, and the image is already cached at the Docker daemon level after the first task launch in each AZ. | 2026-05-05 | Accepted | — |
| DL-0059 | Retain `data.aws_region.current.name` (NOT `.region`) across all Terraform modules under the project's pinned AWS provider constraint `~> 5.70` (versions.tf). DL-0057 was reverted because empirical schema verification of AWS provider versions 5.70.0, 5.79.0, 5.80.0, 5.99.0, and 5.100.0 confirmed that the `.region` attribute on the `aws_region` data source does NOT exist in any 5.x release — it was first introduced in AWS provider 6.0.0. The deprecation warnings observed by the QA Final Checkpoint 13 reviewer (Issue #2, INFO) appeared because their local `.terraform.lock.hcl` had pulled AWS provider 6.43.0 when running `terraform init -backend=false` standalone inside individual module directories without the root composition's `~> 5.70` constraint applying. Under the production CI/CD path (root composition with the constraint enforced), AWS provider 5.x is selected and `.name` produces NO deprecation warnings — the warning is a 6.x-only artifact of standalone module testing. Defer the `.name` -> `.region` migration until a future, separately-decisioned upgrade of the AWS provider major version (5.x -> 6.x), at which point a single-pass migration of all 11 references plus a versions.tf bump will be the correct atomic change. | Bump versions.tf from `~> 5.70` to `~> 6.0` (or `>= 6.0.0, < 7.0.0`) so `.region` becomes available — would force a major-version provider upgrade as a side effect of resolving an INFO-severity finding, an architectural change disproportionate to the issue;<br>introduce a `local.region = data.aws_region.current.name` indirection to make the eventual migration a one-line edit — adds an indirection layer with no operational benefit and does not address the underlying schema mismatch;<br>add `# noqa` style suppressions (Terraform does not support per-line warning suppressions);<br>keep the `.region` substitution in code AND add a `versions.tf` bump (combined: same as the bump-only alternative). | The DL-0057 fix was based on a research error: the search results cited at the time claimed the `.region` attribute was added in AWS provider 5.79+, but empirical verification via `terraform providers schema -json` against pinned AWS provider versions 5.70.0, 5.79.0, 5.80.0, 5.99.0, and 5.100.0 shows that all five expose only `description`, `endpoint`, `id`, `name` — never `region`. Only AWS provider 6.0.0+ exposes both `name` (deprecated) AND `region`. Under our pinned `~> 5.70` constraint (which selects 5.100.0 in practice as of May 2026 and any 5.x patch going forward via Dependabot's standard channel), `.region` is an undefined attribute and `terraform validate` from the root composition fails with "Unsupported attribute" on every reference site (six in `network/vpc_endpoints.tf`, three in `ecs/data.tf`, three in `ecs/locals.tf`, plus `main.tf` line 159 and `migration.tf`). The QA agent's deprecation-warning observation was an artifact of running `terraform init -backend=false` standalone inside module directories, where the root composition's `versions.tf` does not constrain the provider — Terraform pulled the latest 6.x release into `.terraform.lock.hcl` and that 6.x install legitimately deprecates `.name`. Under the actual production deployment path (root composition with `~> 5.70` enforced), the AWS provider 5.x install is used end-to-end, `.name` works, and there are NO deprecation warnings to suppress. The right engineering response is therefore to keep `.name` AND defer the migration to a future versions.tf bump that explicitly opens 6.x adoption — at which point all 11 sites will be migrated atomically in a single PR with its own decision-log entry, accompanied by integration testing for any other AWS provider 6.x breaking changes (the `aws_region` data source is one of dozens of attributes affected by 5.x -> 6.x). DL-0057 is superseded; the QA Issue #2 finding reverts to "INFO, deferred to future provider upgrade" status. The full revert touches every site DL-0057 touched, plus the new `migration.tf` introduced as part of DL-0058's neighboring work, plus the comments in `observability/data.tf`, `alb/data.tf`, and `alb/acm.tf` that DL-0057 also amended for consistency. The Explainability rule (AAP Sec 0.7.5) requires this decision-log entry capturing both the corrected analysis and the empirical evidence (the `terraform providers schema -json` output) so a future contributor does not re-attempt the same mistaken migration. | A future contributor unfamiliar with this analysis may again propose the `.name` -> `.region` migration; mitigated by this decision-log entry, by the explicit "AWS provider `~> 5.70`" version pin in `versions.tf`, and by the Dependabot-driven AWS provider upgrade flow (DL-0026 ecosystem entry) which will surface a concrete decision-log requirement when the team eventually bumps to 6.x. The empirical schema verification could in principle be invalidated by a backport (i.e., AWS provider 5.x maintainers adding `.region` to a 5.x patch release retroactively); mitigated because (a) the AWS provider's 5.x line is in security-fix-only mode as of mid-2025 with no new feature backports, and (b) any such backport would result in `.name` continuing to work as a deprecated alias, so the forward migration would be additive rather than disruptive. The deferred technical debt of the eventual `.name` -> `.region` migration is bounded — exactly 11 lines of code plus 4 comments — and is captured in `docs/onboarding.md`'s Suggested Next Tasks section so a future contributor can pick it up alongside the AWS provider 6.x upgrade. | 2026-05-05 | Accepted | — |
| DL-0060 | Place the new `frontend/src/components/README.md` at the literal prompt-quoted path even though the F-002 AI form (`AddEditConnectionForm.tsx`) actually resides at `frontend/src/features/connections/AddEditConnectionForm.tsx`. The README documents the design-system primitives that live under `frontend/src/components/ui/` (Button, Input, Textarea, Toast, etc.) and includes an explicit "Path Aliases" cross-reference pointing readers to the actual AI form host. | Place the README inside `frontend/src/features/connections/`;<br>move the form file to `frontend/src/components/` to match the prompt-quoted path;<br>omit the README entirely and document the AI-form integration only in the deep-dive. | Respects the user's literal path specification while honoring the AAP § 0.2.1 minimal-change clause ("Do not refactor, optimize, rename, or change existing interfaces. Document existing code as-is"). The `frontend/src/components/` directory genuinely hosts the design-system primitives that the AI button composes, so a README there is meaningful even before the cross-reference is considered. Sibling-agent work in `frontend/src/lib/README.md` and the F-002 deep-dive `docs/ai-note-generation-workflow.md` reference DL-0060 by name; placing the README at the literal path keeps that cross-reference accurate. | Developers searching `features/connections/` for documentation may miss the README in `components/`; mitigated by (a) the explicit Path Aliases subsection at the top of the README naming `frontend/src/features/connections/AddEditConnectionForm.tsx`, (b) the corresponding entry in the `docs/onboarding.md` "AI Workflow Documentation Map" subsection, and (c) the deep-dive's § 3 (Frontend Flow) which cites the actual file path in every code reference. | 2026-05-27 | Accepted | — |
| DL-0061 | Place the new `frontend/src/lib/README.md` at the literal prompt-quoted path even though the F-002 AI-notes mutation hook actually resides at `frontend/src/api/notes.ts` and the shared transport at `frontend/src/api/client.ts`. The README documents the cross-cutting browser utilities that live under `frontend/src/lib/` (`correlationId.ts`, `queryClient.ts`, `localStore.ts`) and includes an explicit Path Aliases cross-reference pointing readers to the actual AI-notes API client files. | Place the README inside `frontend/src/api/`;<br>move the API client files into `frontend/src/lib/`;<br>omit the README and document the API client surface only in the deep-dive. | Respects the user's literal path specification while honoring the minimal-change clause. The `frontend/src/lib/` directory genuinely hosts the `correlationId.ts` module that IS directly relevant to F-002 (the `X-Correlation-Id` propagation surface), and the `queryClient.ts` module is the TanStack Query singleton that hosts the cache for `useGenerateNotesMutation`. The README serves these meaningfully even before the cross-reference is considered. The `blitzy-deck/index.html` update and the `frontend/src/components/README.md` also cite DL-0061 by name; placing the README at the literal path keeps every cross-reference accurate. | Developers searching `frontend/src/api/` for utility documentation may miss the README in `lib/`; mitigated by (a) the Path Aliases subsection in `frontend/src/lib/README.md` naming the actual files, (b) the corresponding entry in `docs/onboarding.md`'s Documentation Map, and (c) the deep-dive's § 3 and § 8 which cite the actual file paths. | 2026-05-27 | Accepted | — |
| DL-0062 | Apply targeted inline docstring updates (PEP 257 for Python, TSDoc/JSDoc for TypeScript) to the ACTUAL source filenames (`backend/app/services/ai_orchestration.py`, `backend/app/api/notes.py`, `frontend/src/features/connections/AddEditConnectionForm.tsx`, `frontend/src/api/notes.ts`, `frontend/src/api/client.ts`) rather than the prompt-quoted aliases (`ai_orchestrator.py`, `ConnectionForm.tsx`, `lib/api.ts`). | Rename the actual files to match the prompt-quoted names (would require renaming `ai_orchestration.py` to `ai_orchestrator.py`, moving `AddEditConnectionForm.tsx` to `frontend/src/components/ConnectionForm.tsx`, and consolidating `notes.ts` + `client.ts` into `frontend/src/lib/api.ts`);<br>skip the inline-doc updates entirely until the files are renamed in a separate refactor. | File renames would break every import statement across the codebase (and every existing test) — a refactor disproportionate to a documentation delivery and a direct violation of the AAP § 0.2.1 minimal-change clause: "Do not modify production code logic or behavior. Do not refactor, optimize, rename, or change existing interfaces. Document existing code as-is." The prompt-quoted names are aliases captured here so a future contributor searching for those names through this decision log can find the actual files immediately. | A reader of the original prompt may search for files under the quoted names (`ai_orchestrator.py`, `ConnectionForm.tsx`, `lib/api.ts`) and find nothing; mitigated by (a) this decision-log entry being the authoritative resolver of the alias, (b) explicit Path Aliases or "Note on file paths" subsections in each new module README, and (c) the onboarding Documentation Map listing both the prompt-quoted README path and the actual touched source file. | 2026-05-27 | Accepted | — |
| DL-0063 | Author all documentation for this deliverable as plain GitHub-Flavored Markdown with embedded Mermaid diagrams; do NOT introduce a documentation generator framework (MkDocs, Sphinx, Docusaurus, TypeDoc, or similar). | Stand up MkDocs Material to aggregate the README files, deep-dive, and existing `docs/` content into a searchable site with auto-generated navigation;<br>adopt Sphinx with the `myst-parser` extension to render the Markdown source plus auto-generate Python API documentation from the existing docstrings;<br>adopt Docusaurus for a richer landing-page experience. | The existing `docs/` folder is already authored as plain Markdown with embedded Mermaid (verified during repository inspection: `docs/api.md`, `docs/architecture.md`, `docs/security.md`, `docs/operations.md`, `docs/onboarding.md`, plus the standalone diagrams under `docs/diagrams/*.mmd`). GitHub renders both Markdown and Mermaid natively in the repository UI. Introducing a generator would add tooling, CI configuration, a build artifact, and a hosting concern — all of which conflict with the AAP § 0.2.1 minimal-change clause and would require new decision-log entries for each (framework choice, build-step choice, hosting choice). Plain Markdown preserves zero-step rendering in GitHub, in every IDE Markdown previewer, and on the existing PR diff surface. | No aggregated full-text search across the documentation surface; mitigated by GitHub's repository-wide code search, IDE search across the workspace, and the cross-link discipline established by the onboarding Documentation Map and the deep-dive's § 12 References. | 2026-05-27 | Accepted | — |
| DL-0064 | Place the F-002 deep-dive document at `docs/ai-note-generation-workflow.md` (sibling of `api.md`, `architecture.md`, `security.md`, `operations.md`) rather than inside a module folder such as `backend/app/services/`. | Place the deep-dive inside `backend/app/services/` alongside the AI orchestrator (e.g., `backend/app/services/ai-note-generation.md`);<br>place it inside `frontend/src/features/connections/` alongside the form host;<br>split it into separate per-layer deep-dives (one in `backend/`, one in `frontend/`). | The F-002 workflow is inherently cross-cutting — it spans SPA, API, service, and provider layers across two languages and at least four code locations. Placing the deep-dive at the `docs/` root makes it discoverable from the same canonical location as the system-wide contracts (`api.md`, `architecture.md`, `security.md`, `operations.md`), so a contributor who needs the end-to-end story finds it in the same folder they already use for system-level questions. The `docs/onboarding.md` Documentation Map links to this canonical location; the four sibling module READMEs link to this canonical location; the `blitzy-deck/index.html` documentation-delivery slide links to this canonical location. | Divergence between the deep-dive and the four module READMEs over time as individual files evolve; mitigated by bidirectional cross-references — each module README's "Related Modules" section links to the deep-dive, and the deep-dive's § 12 References lists every cited source file with its current line numbers — so a future contributor making a change to either side is prompted by the references to update the other. | 2026-05-27 | Accepted | — |
| DL-0065 | Make NO functional change to any production code as part of this F-002 documentation deliverable. The only source-file modifications are PEP 257 docstrings (Python) and TSDoc/JSDoc blocks (TypeScript) added to existing public symbols; constants, control flow, imports, type signatures, and behavior are preserved byte-for-byte equivalent in observable behavior. | Refactor `frontend/src/api/client.ts` (637 lines) by splitting it into transport / error-mapping / auth-redirect modules while documenting;<br>refactor `frontend/src/features/connections/AddEditConnectionForm.tsx` (841 lines) by extracting field-group sub-components while documenting;<br>introduce a dedicated `AIPromptTooLargeError` exception (currently subsumed under `AIServiceUnavailableError`) while documenting. | The AAP § 0.2.1 minimal-change clause is explicit and absolute: "Make only the changes absolutely necessary to implement comprehensive code documentation. Add README files, one workflow deep-dive document, and targeted inline comments/docstrings only. Do not modify production code logic or behavior. Do not refactor, optimize, rename, or change existing interfaces. Document existing code as-is." Each refactor alternative would touch multiple test files, require a follow-up review pass to confirm no behavior change, and conflict with the user's intent to deliver documentation in isolation. Each refactor is captured as a "Suggested Next Tasks" entry in `docs/onboarding.md` so a future contributor can pick it up under its own change-control envelope (and its own decision-log entry). | The documentation describes existing (unrefactored, single-file) code surfaces that some future reviewers may consider unidiomatic; mitigated because (a) the documentation accurately reflects the current shipped behavior, (b) the suggested refactors are explicitly catalogued in `docs/onboarding.md` for future work, and (c) the inline docstrings live alongside the code so any future refactor automatically inherits the documentation that survives the move. | 2026-05-27 | Accepted | — |

## See Also

- [`README.md`](../README.md) — project entry point and quick-start.
- [`onboarding.md`](onboarding.md) — clean-machine to running app, domain glossary, common pitfalls, suggested next tasks.
- [`architecture.md`](architecture.md) — what the architecture is (this log explains why).
- [`api.md`](api.md) — REST endpoint catalog.
- [`operations.md`](operations.md) — deploys, rollbacks, secret rotation, dashboards.
- [`security.md`](security.md) — authentication, RBAC, audit, secrets, data scoping.
- `.github/pull_request_template.md` — pull request template that enforces an entry in this log via checklist for every non-trivial change.
